<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://hbell99.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hbell99.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-09-02T12:12:33+00:00</updated><id>https://hbell99.github.io/feed.xml</id><title type="html">Huayu Deng’s Page</title><subtitle>邓画予 (Huayu Deng)&apos;s personal academic page.
</subtitle><entry xml:lang="zh"><title type="html">OPD 中 reverse KL 的 in Reward 与 in Loss</title><link href="https://hbell99.github.io/reinforcement-learning/2026/09/03/opd-reverse-kl-in-reward-vs-in-loss.html" rel="alternate" type="text/html" title="OPD 中 reverse KL 的 in Reward 与 in Loss" /><published>2026-09-03T00:00:00+00:00</published><updated>2026-09-03T00:00:00+00:00</updated><id>https://hbell99.github.io/reinforcement-learning/2026/09/03/opd-reverse-kl-in-reward-vs-in-loss</id><content type="html" xml:base="https://hbell99.github.io/reinforcement-learning/2026/09/03/opd-reverse-kl-in-reward-vs-in-loss.html"><![CDATA[<p>这篇算是<a href="/reinforcement-learning/2026/09/02/kl-divergence-correct-form.html">上一篇 KL 笔记</a>的姐妹篇。上一篇讲的是 RLHF / GRPO 里”KL 正则化”这个场景：给任务 reward 加一个”别跑太远”的惯罚项。这一篇要讲的是 Thinking Machines 那篇 <a href="https://thinkingmachines.ai/blog/on-policy-distillation/">On-Policy Distillation</a>（下面简称 OPD）——一个表面上很不一样，但骨子里用的是同一套数学的场景：这次 reverse KL 不是”加在任务 reward 上的惯罚项”，它<strong>就是</strong>整个训练信号本身。</p>

<p>正因为两篇用的是同一套底层数学，上一篇里六宫格、k1/k2/k3 的所有结论可以几乎不打折扣地搬过来用——但 OPD 的具体设计（尤其是 discount=0、以及教师只给 top-k logprobs 这个工程现实）又引出几个上一篇没碰到的新问题。这篇就写这几个新问题。</p>

<hr />

<h2 id="一opd-是什么它的-reverse-kl-长什么样">一、OPD 是什么，它的 reverse KL 长什么样</h2>

<p>先把 OPD 的设定说清楚，免得后面的分析没有落脚点。</p>

<p>Thinking Machines 把后训练方法按”采样方式”和”奖励密度”分成三类：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th> </th>
      <th>采样</th>
      <th>奖励密度</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>SFT</td>
      <td>off-policy（数据来自别处）</td>
      <td>dense（每个 token 都有监督信号）</td>
    </tr>
    <tr>
      <td>RL</td>
      <td>on-policy（数据来自当前策略）</td>
      <td>sparse（一条轨迹一个标量奖励）</td>
    </tr>
    <tr>
      <td><strong>OPD</strong></td>
      <td><strong>on-policy</strong></td>
      <td><strong>dense</strong></td>
    </tr>
  </tbody>
</table></div>

<p>OPD 想要的是”RL 的 on-policy 采样”和”distillation 的逐 token 密集信号”两头的好处：从学生 ⟦INLMATH10MATHEND⟧ 自己采样出一条轨迹，然后让一个更强的教师 ⟦INLMATH11MATHEND⟧ 给轨迹里<strong>每一个 token</strong> 打分——类似棋类引擎给每一步棋标注”这步是漏着/失误/精彩”，而不是等一整局下完才给一个胜负。</p>

<p>具体的训练信号是<strong>逐 token 的反向 KL</strong>：</p>

<p>⟦DISPMATH1MATHEND⟧</p>

<p>（这就是上一篇定义的 ⟦INLMATH12MATHEND⟧，只是把”ref”换成了”teacher”。）他们把这个量的负值直接设成每个 token 的 advantage：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reverse_kl</span> <span class="o">=</span> <span class="n">sampled_logprobs</span> <span class="o">-</span> <span class="n">teacher_logprobs</span>   <span class="c1"># 就是 k1(y_t)
</span><span class="n">trajectories</span><span class="p">[</span><span class="sh">"</span><span class="s">advantages</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="o">-</span><span class="n">reverse_kl</span>
<span class="c1"># 套用已有 RL 脚本的 importance-sampling loss 完成训练
</span><span class="n">training_client</span><span class="p">.</span><span class="nf">forward_backward</span><span class="p">(</span><span class="n">trajectories</span><span class="p">,</span> <span class="n">loss_fn</span><span class="o">=</span><span class="sh">"</span><span class="s">importance_sampling</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>选反向 KL（而不是正向 KL，或者别的散度）不是随手选的，博客给了两个理由，值得记一下：</p>

<ul>
  <li><strong>mode-seeking，”unhackable”</strong>：反向 KL 只要求学生在自己生成的地方跟老师对齐，不要求覆盖老师所有的模式。这意味着低 KL 总对应老师视角下的高质量行为——不像奖励模型，模型没法通过”钻 KL 估计器的空子”来刷分。</li>
  <li><strong>per-token、无需等完整 rollout</strong>：算这一项只需要教师做<strong>一次前向传播</strong>（给定学生已经采出的序列，一次 forward 就能拿到每个位置的 logprob），不需要教师自己生成。而且他们特意把 discount factor 设成 <strong>0</strong>——这一点很关键，第三节会展开。</li>
</ul>

<p>博客里还有一句容易被忽略但对本文很重要的话：他们<strong>没有</strong>在实验里用”logit（top-k）distillation”，只说这是”可以进一步提升算力效率的方向”，并且声称”对序列采样求期望，和直接对老师完整分布做 logit distillation，理论上是同一个目标的无偏估计”。这句话对不对、在什么条件下对，是第四节要拆开讲的。</p>

<hr />

<h2 id="二opd-的-reverse-kl-落在六宫格的哪一格">二、OPD 的 reverse KL 落在六宫格的哪一格</h2>

<p>上一篇笔记最核心的结论是一张六宫格：k1/k2/k3 三个估计器，分别放进”KL in Reward”（IR，梯度只保留得分函数项）和”KL in Loss”（IL，梯度只保留路径导数项）两种位置，梯度是否精确等于真梯度，结论完全不对称。现在把 OPD 的实现对进这张格子里。</p>

<h3 id="21-opd-用的就是-k1-in-reward而且是六宫格里唯一永远对的格子">2.1 OPD 用的就是 k1-in-Reward——而且是六宫格里唯一”永远对”的格子</h3>

<p>看 OPD 的代码：<code class="language-plaintext highlighter-rouge">reverse_kl</code> 是在 <code class="language-plaintext highlighter-rouge">torch.no_grad()</code> 语境下算出来的一个数值（教师 client 只做推理，不参与反传；学生的 <code class="language-plaintext highlighter-rouge">sampled_logprobs</code> 也是采样时刻的、被当作快照使用的），然后被塞进 <code class="language-plaintext highlighter-rouge">advantages</code>，走标准的 RL importance-sampling loss（即用 ⟦INLMATH13MATHEND⟧ 这个比值乘 advantage，只对 ⟦INLMATH14MATHEND⟧ 里的 ⟦INLMATH15MATHEND⟧ 反传）。</p>

<p>这精确对应上一篇 §4.1 的 <strong>k1-in-Reward</strong>：advantage 里的 KL 值被 stop-grad，只有得分函数项（通过 ⟦INLMATH16MATHEND⟧）在反传。而 §4.1 证明过一个很强的结论——k1 的路径导数期望恒为 0，所以 <strong>k1-in-Reward 的梯度和”完整、正确”的反向 KL 梯度精确相等，不是近似</strong>：</p>

<p>⟦DISPMATH2MATHEND⟧</p>

<p>也就是说，OPD 的 reward 单独存在的话（把它想象成”⟦INLMATH17MATHEND⟧、只有 KL 惩罚”的特例代入上一篇的框架），<strong>在梯度层面精确等于对 ⟦INLMATH18MATHEND⟧ 做梯度下降</strong>，没有任何近似、没有任何因为”用 RL 脚手架实现”而引入的偏差。OPD 的作者在博客里说”我们的实现相对于已有的、带 KL 正则化的 RL 实现只是一行代码的改动”——现在可以更精确地说：<strong>这一行代码改动，恰好把训练目标从”任务 reward 减去 k1 惩罚”换成了”纯粹的 k1 惩罚”，而两种写法在梯度精确性上是同一个量级的——都精确无偏，因为改动前后都停在了六宫格里同一格。</strong></p>

<p>这不只是走运。选反向 KL、走 RL 的 advantage/重要性采样通道，这两个设计决策合在一起，正好落在了上一篇六宫格里”永远对”的那一格。</p>

<h3 id="22-如果图省事把它写成-loss会精确复现梯度恒为-0的陷阱">2.2 如果图省事把它写成 loss，会精确复现”梯度恒为 0”的陷阱</h3>

<p>现在设想一个很容易冒出来的”简化”想法：既然目标就是让学生逼近老师，何必绕一圈套 RL 的 advantage/importance-sampling 脚手架？直接把逐 token 的 ⟦INLMATH19MATHEND⟧ 当成一个 loss，对着它反传不就完了？</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 一个看起来更直接、但是错的写法
</span><span class="n">y</span> <span class="o">=</span> <span class="nf">sample_from</span><span class="p">(</span><span class="n">student</span><span class="p">,</span> <span class="n">no_grad</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>          <span class="c1"># 采样本身不可微，天然 stop-grad
</span><span class="n">loss</span> <span class="o">=</span> <span class="p">(</span><span class="n">student</span><span class="p">.</span><span class="nf">logprob</span><span class="p">(</span><span class="n">y</span><span class="p">)</span> <span class="o">-</span> <span class="n">teacher</span><span class="p">.</span><span class="nf">logprob</span><span class="p">(</span><span class="n">y</span><span class="p">)).</span><span class="nf">mean</span><span class="p">()</span>
<span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
</code></pre></div></div>

<p>这正是上一篇 §4.2 的 <strong>k1-in-Loss</strong>：采样分布的 ⟦INLMATH20MATHEND⟧ 被 stop-grad（因为采样这一步本来就不可微），但 log-prob 项里的 ⟦INLMATH21MATHEND⟧ 保留梯度。而 §4.2 证明的结论是致命的：</p>

<p>⟦DISPMATH3MATHEND⟧</p>

<p><strong>这个 loss 的梯度期望恒为 0。</strong> 训练看起来在跑（loss 数值会动，因为它记录的是当前采样批次的偶然波动），但从统计意义上讲，<strong>这个 loss 不会把学生往老师那边推一寸</strong>。这是本文想强调的第一条、也最容易在”重新实现一遍 OPD、觉得 RL 脚手架太重”时踩的坑：<strong>OPD 的正确性不是来自”用了反向 KL”，而是来自”反向 KL 被放对了位置”。把同一个量从 reward 挪到 loss，看起来只是搬了个地方，梯度性质却从”精确无偏”变成”精确的零”。</strong></p>

<p>（如果你想知道”为什么不干脆用交叉熵蒸馏 loss（即标准的、off-policy 的 logit distillation，教师给分布、学生对着算 cross entropy）”——那是另一件事，那个 loss 确实有效，但它对应的是<strong>正向 KL</strong>、<strong>off-policy 采样</strong>，训练动态和 OPD 完全不同，不在本文讨论范围内。本文只讨论”想要 on-policy 采样 + reverse KL”这个组合下，reward 和 loss 两种放法的区别。）</p>

<h3 id="23-一个更隐蔽的冲动为了降方差换成-k3-in-reward这是-opd-里最不该做的事">2.3 一个更隐蔽的冲动：为了降方差换成 k3-in-Reward——这是 OPD 里最不该做的事</h3>

<p>⟦INLMATH22MATHEND⟧ 的缺点在上一篇讲过：单样本方差极大，尤其是分布越接近、信噪比越差。OPD 训练到后期，学生已经很接近老师了，这时候 ⟦INLMATH23MATHEND⟧ 的方差问题会很扎眼——直觉上很容易想到”换成 ⟦INLMATH24MATHEND⟧，无偏、非负、方差小三个都要”。</p>

<p><strong>这恰恰是 OPD 场景下最不该做的替换。</strong> 上一篇 §4.4 已经证明并用数值验证过：</p>

<p>⟦DISPMATH4MATHEND⟧</p>

<p>两项在 ⟦INLMATH25MATHEND⟧ 时到二阶为止相等，<strong>一阶抵消</strong>。上一篇的数值表（换成这里的记号）：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>学生 vs 老师的差距</th>
      <th>真梯度模长</th>
      <th>k3-in-Reward 梯度模长</th>
      <th>剩余比例</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>大</td>
      <td>1.76e-1</td>
      <td>3.04e-2</td>
      <td>17%</td>
    </tr>
    <tr>
      <td>小</td>
      <td>1.52e-2</td>
      <td>7.54e-4</td>
      <td>5.0%</td>
    </tr>
    <tr>
      <td>很小</td>
      <td>3.03e-2</td>
      <td>2.25e-4</td>
      <td><strong>0.74%</strong></td>
    </tr>
  </tbody>
</table></div>

<p>问题在于：<strong>OPD 的整个训练过程就是在把”学生 vs 老师的差距”从大推向很小</strong>——这正是 k3-in-Reward 梯度塌陷最严重的方向。用 k3 当 OPD 的 reward，train 到后期（也就是最需要精细打磨的阶段），你会发现梯度信号几乎完全消失，而且不会有任何报错——loss/KL 监控数值看起来一切正常（k3 的值本身还是低方差、非负的），只是模型不再往老师那边靠近了。这比”方差大但至少方向对”的 ⟦INLMATH26MATHEND⟧ 危险得多。</p>

<p><strong>结论：OPD 场景下 ⟦INLMATH27MATHEND⟧-in-Reward 的高方差是必须忍受的代价，不能用 ⟦INLMATH28MATHEND⟧ 抄近路。</strong> 如果真的要降方差，方向应该是标准的 RL 方差缩减手段（group baseline、per-prompt 归一化、多样本平均），而不是换估计器本身。</p>

<hr />

<h2 id="三discount--0故意不要-part-2">三、Discount = 0：故意不要 Part 2</h2>

<p>上一篇 §5 有一个重要发现：把 KL 惩罚折进 reward、走标准 policy gradient 的 reward-to-go，会<strong>自动</strong>把”当前 token 对后续所有 token KL 的级联影响”（Part 2）计入当前 token 的梯度——这被认为是 KL-in-Reward 的一个结构性优势，因为大多数 KL-in-Loss 的实现都漏掉了这一项。</p>

<p>OPD 明确反着来：博客里说”我们没发现 discount factor &gt; 0 能提升效果，所以选择了 0”。Discount = 0 意味着 ⟦INLMATH29MATHEND⟧ 位置的 return 就是 ⟦INLMATH30MATHEND⟧ 本身，<strong>不会</strong>累加任何未来 token 的信号——<strong>Part 2 被主动关掉了</strong>。</p>

<p>这不是疏忽，是设计选择，而且是有道理的：</p>

<ul>
  <li><strong>KL 正则化想要 Part 2</strong>，因为它关心的是”整条轨迹”离参考模型有多远，当前 token 选择会把后续生成引到什么样的分布区域去，这个级联效应正是你想控制的东西。</li>
  <li><strong>OPD 不想要 Part 2</strong>，因为它的比喻是”棋步评分”：每一步的好坏应该独立打分，不应该因为后面几步走飞了就往前面这步身上倒扣分（也不应该后面走好了就给前面这步加分）。这是标准的 credit assignment 诉求——你想知道的是”这一步本身选得好不好”，而不是”这一步选完之后整条轨迹变成什么样”。开启 Part 2 会让每个 token 的训练信号变成”自己的 KL + 一堆跟自己没有直接因果关系、只是恰好排在后面的 token 的 KL”，这会显著拉低信噪比，也会让 credit assignment 变得模糊。</li>
</ul>

<p>一句话总结这个对比：<strong>同一个”reward-to-go 自动带不带 Part 2”的性质，在 KL 正则化里是优点，在 OPD 里如果不设 ⟦INLMATH31MATHEND⟧ 就会变成缺点。</strong> 这也是个提醒：六宫格的结论是”哪个格子梯度精确”，但”你到底想不想要 Part 2 那个级联项”是另一个独立的设计选择，取决于你的训练信号本身想表达什么语义，不能照抄。</p>

<hr />

<h2 id="四top-k-实现要注意的地方">四、Top-k 实现要注意的地方</h2>

<p>这一节是本文的第二个主题，也是纯工程但很容易被忽略的一块。</p>

<h3 id="41-k1以及-k3天然只需要一个点不需要整条分布">4.1 k1（以及 k3）天然只需要”一个点”，不需要整条分布</h3>

<p>先说一个好消息。仔细看 ⟦INLMATH32MATHEND⟧——它只涉及<strong>采样到的那一个 token</strong> 在两个分布下的概率，不涉及对整条词表求和。这跟”logit distillation”（也叫 dense KD，在每个位置对<strong>整条</strong>教师分布做加权，比如 ⟦INLMATH33MATHEND⟧）是本质不同的计算模式：</p>

<ul>
  <li><strong>k1/k3 式（OPD 的做法）</strong>：每个位置只需要教师给出”采样到的那个 token”的一个 logprob 数值。</li>
  <li><strong>logit distillation 式</strong>：每个位置需要教师给出整条词表的分布（或者至少覆盖大部分概率质量的一个子集）。</li>
</ul>

<p>这个区别决定了两者对”教师只暴露 top-k logprobs”（很多推理服务只返回 top-k，出于带宽或接口限制）这件事的敏感程度完全不同。<strong>k1/k3 式的方法天然对 top-k 更宽容</strong>：只要采样到的 token 落在教师返回的 top-k 列表里，你就拿到了精确的 ⟦INLMATH34MATHEND⟧，跟教师给不给你剩下几万个 token 的概率完全无关。</p>

<p>（这也是 Thinking Machines 那句”序列采样是老师完整分布的无偏估计，理论上是同一个目标”的准确含义：把 ⟦INLMATH35MATHEND⟧ 对 ⟦INLMATH36MATHEND⟧ 求期望，恰好就等于 logit distillation 在该位置算出的那个精确的逐位置反向 KL 值——见上一篇 §1 对 k1 无偏性的证明，这里只是换了个”ref”的名字。序列采样版本方差更大，但期望上是同一个目标，这个等价关系没有问题。）</p>

<h3 id="42-但宽容是有代价的学生犯大错的时候恰好最容易缺数据">4.2 但”宽容”是有代价的：学生犯大错的时候恰好最容易缺数据</h3>

<p>问题出在 top-k 之外。学生训练早期、或者某次采样运气不好，完全可能采到一个<strong>教师认为极不可能</strong>的 token——而”教师认为极不可能”恰恰意味着这个 token 大概率不在教师返回的 top-k 列表里。<strong>这是最需要精确惯罚的时刻，却也是信息最缺失的时刻。</strong></p>

<p>具体会有多缺失？做个数值实验：词表 2000，教师分布稍微peaky（前几个 token 概率明显更高），只暴露 top-20：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>教师 top-20 概率质量占比: 0.4270（尾部质量 0.5730——真实场景通常比这更集中，这里故意选了个分布更平的例子来放大问题）
</code></pre></div></div>

<p><strong>在 top-k 内部</strong>，如果用”除以 top-k 概率总和”做归一化（一个很自然的、让概率重新加起来等于 1 的实现方式），会给<strong>每一个</strong> in-top-k token 的 log-prob 加上完全相同的常数偏移：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>第 1 名 token：真实 log p=-1.9111   renorm 后=-1.0601   偏移 = +0.851
第 5 名 token：真实 log p=-4.3041   renorm 后=-3.4531   偏移 = +0.851
第20名 token：真实 log p=-5.7293   renorm 后=-4.8783   偏移 = +0.851
</code></pre></div></div>

<p>偏移量恒等于 ⟦INLMATH37MATHEND⟧，跟具体是哪个 token 无关——这一点其实还好，因为它是个<strong>常数</strong>：如果学生几乎所有采样都落在 top-k 内部，这个常数偏移只是把每个 token 的 ⟦INLMATH38MATHEND⟧ 整体加了个偏置，相当于给这条轨迹加了个常数 baseline，advantage 里的相对大小关系没变，对训练方向影响有限（虽然会让 KL 监控数值系统性偏低，不能直接拿去做绝对量级的判断）。</p>

<p><strong>真正麻烦的是 top-k 之外。</strong> 常见的偷懒实现是：查不到就退化成”clip 到第 k 名的概率”或者”把尾部质量平摊到剩余词表”。数值上看这两种退化会怎样低估惩罚：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>一个"学生犯大错"的例子：采到全词表里概率倒数第一的 token
  真实 log p = -12.7843
  若 clip 成第20名的概率(log p=-5.7293)：k1 被低估了 7.06 nat
</code></pre></div></div>

<p><strong>低估 7 个 nat 意味着什么？</strong> 意味着这个本该被重罚的 token，它对应的负 ⟦INLMATH39MATHEND⟧（也就是 advantage）会比真实值小了 ⟦INLMATH40MATHEND⟧ 倍量级的惩罚力度（在 log 空间差 7，概率空间就是三个数量级）。也就是说：<strong>学生犯的错越离谱，你手上的估计器就越可能失效，而且失效的方向永远是”往小了估”</strong>——这是一个系统性的、方向确定的偏差,不是随机噪声,标准的方差缩减手段（多采样平均）救不了它,因为每次采样只要落在 top-k 之外都会遇到同样方向的低估。</p>

<h3 id="43-三种-fallback-策略选哪个">4.3 三种 fallback 策略，选哪个</h3>

<p>汇总一下工程上常见的三种处理方式，以及各自的问题：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>策略</th>
      <th>做法</th>
      <th>问题</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>当成概率 0</td>
      <td>⟦INLMATH41MATHEND⟧</td>
      <td>⟦INLMATH42MATHEND⟧，advantage 炸穿，训练直接 NaN。<strong>不能用。</strong></td>
    </tr>
    <tr>
      <td>Clip 到第 k 名</td>
      <td>⟦INLMATH43MATHEND⟧</td>
      <td>数值稳定，但系统性低估惩罚，且犯错越大低估越狠（上面的例子）。</td>
    </tr>
    <tr>
      <td>尾部质量平摊</td>
      <td>⟦INLMATH44MATHEND⟧</td>
      <td>比 clip 更接近”假设尾部均匀”这个（通常错误的）假设，实践中跟 clip 量级差不多，一样是低估，只是低估程度依赖 ⟦INLMATH45MATHEND⟧ 的估计是否准确。</td>
    </tr>
    <tr>
      <td><strong>额外单独查询该 token 的精确 logprob</strong></td>
      <td>发现采样 token 不在 top-k 里时，单独再问教师要这一个 token 的精确 logprob（大多数推理服务的 logprobs 接口支持”给定 token 查其 logprob”，不需要重新做完整的 top-k 排序）</td>
      <td>需要多一次（通常很便宜的）教师查询，但是唯一不引入系统性偏差的方案。<strong>只要接口支持，应该优先选这个。</strong></td>
    </tr>
  </tbody>
</table></div>

<p>如果接口完全不支持”查指定 token 的 logprob”（只能拿 top-k），退而求其次要在 clip 和尾部平摊里选一个时，倾向于选<strong>尾部平摊</strong>并且把 ⟦INLMATH46MATHEND⟧（有效词表大小，可以用分词器词表减去明显不会被采样到的特殊 token）估得保守一点——这样至少低估的量级是可控、可解释的，而不是像 clip 那样，”低估多少”完全取决于第 k 名概率恰好多大，跟真实的犯错程度没有关系。</p>

<h3 id="44-如果做的是-logit-distillationdense-loss偏差机制是另一种">4.4 如果做的是 logit distillation（dense loss），偏差机制是另一种</h3>

<p>上面讨论的都是”OPD 式、逐点采样”的场景。如果你选择了 Thinking Machines 提到的另一条路——直接做 logit distillation，在每个位置对教师的<strong>整条</strong>分布求和，那 top-k 截断造成的偏差是另一种性质：<strong>不是”某个点的数据缺失”，而是”整条分布被系统性削尖”。</strong></p>

<p>具体说：如果只用 top-k 内的 token 计算 ⟦INLMATH47MATHEND⟧（把尾部直接丢弃，不做任何补偿），相当于在做一个”条件在 top-k 上”的近似 KL，丢弃的尾部概率质量越大，这个近似跟真实反向 KL 的差距越大——而且这个偏差是<strong>在所有位置上都存在的系统性偏差</strong>，不像 4.2 里的”只在学生犯大错时才现形”。这种偏差通常还需要配合 §2 的”in reward / in loss”选择一起看：如果这个截断后的量被拿去做 reward（k1-in-Reward 式的 IS），路径导数为零的那个”免疫”性质（§2.1 的核心论证）并不依赖于”用的是精确 KL 还是截断近似”——只要这个量本身不含 ⟦INLMATH48MATHEND⟧ 的路径导数（即：教师分布和截断集合的选取都跟 ⟦INLMATH49MATHEND⟧ 无关，只由采样到的 ⟦INLMATH50MATHEND⟧ 决定），stop-grad 免疫这条结论依然成立；但截断带来的<strong>数值上的系统性偏差</strong>（把 reward 的绝对量级估歪）不会因为”放对了位置”而消失，这是两个独立的问题，不要混为一谈。</p>

<h3 id="45-把-in-reward--in-loss-搬到-top-k-场景下重新推一遍">4.5 把 in-Reward / in-Loss 搬到 top-k 场景下重新推一遍</h3>

<p>前面几节讨论的是”单点采样时教师给不给你那个具体 token 的 logprob”这个数据缺失问题。这里换一个角度：如果不满足于单点采样（想降低方差），而是把更新<strong>摊到整个候选集合 ⟦INLMATH51MATHEND⟧ 上</strong>（⟦INLMATH52MATHEND⟧ 可以是任意一种 top-k，先不纠结是谁的 top-k），会发现 §2 那套”in Reward 精确、in Loss 会出问题”的故事在这里重演了一遍，而且能推出一个更精确的定量关系。</p>

<p>先把两种写法在这个”摊到整个 ⟦INLMATH53MATHEND⟧”的设定下重新定义。记 ⟦INLMATH54MATHEND⟧ 上学生分配的总概率质量为</p>

<p>⟦DISPMATH5MATHEND⟧</p>

<p><strong>in-Loss（截断求和，直接反传）</strong>：把反向 KL 截断到 ⟦INLMATH55MATHEND⟧ 上，直接对这个截断和求梯度：</p>

<p>⟦DISPMATH6MATHEND⟧</p>

<p><strong>in-Reward/Adv（每个候选 token 单独算 ratio，advantage 在 ⟦INLMATH56MATHEND⟧ 内部重新归一化、并 stop-grad）</strong>：先把 ⟦INLMATH57MATHEND⟧ 内的学生概率重新归一化成一个”局部分布” ⟦INLMATH58MATHEND⟧，用它加权 ⟦INLMATH59MATHEND⟧ 作为（stop-grad 的）advantage，再走标准的重要性采样 PG loss。</p>

<p>按 §2.1 同样的套路（advantage 停梯度，只有 ratio 那条路径反传；on-policy 时 ratio 的梯度化简为 ⟦INLMATH60MATHEND⟧），这个写法的梯度是纯得分函数项：</p>

<p>⟦DISPMATH7MATHEND⟧</p>

<p>再把 ⟦INLMATH61MATHEND⟧ 用乘积法则展开（⟦INLMATH62MATHEND⟧，教师项不含 ⟦INLMATH63MATHEND⟧）：</p>

<p>⟦DISPMATH8MATHEND⟧</p>

<p>对比这两个式子，把第一个代进去，得到两者的精确关系（我做过数值验证，用有限差分核对，残差在 ⟦INLMATH64MATHEND⟧ 量级）：</p>

<p>⟦DISPMATH9MATHEND⟧</p>

<p>这里有个容易被忽略的细节：两者的差距<strong>不只是</strong>多出一个 ⟦INLMATH65MATHEND⟧ 项，共享的那部分得分函数项之间还差了一个 ⟦INLMATH66MATHEND⟧ 的<strong>尺度因子</strong>——因为 in-Reward 用的是 ⟦INLMATH67MATHEND⟧ 内部重新归一化过的概率 ⟦INLMATH68MATHEND⟧，in-Loss 用的是原始的 ⟦INLMATH69MATHEND⟧，两者天然差 ⟦INLMATH70MATHEND⟧ 倍。</p>

<p><strong>全词表</strong>（⟦INLMATH71MATHEND⟧）时，⟦INLMATH72MATHEND⟧ 恒成立（softmax 归一化的定义），于是 ⟦INLMATH73MATHEND⟧ 且尺度因子也等于 1——两个差异同时消失，两种写法梯度精确相等。这其实是 §2.1”k1 路径导数期望为 0”这条结论在”对 ⟦INLMATH74MATHEND⟧ 精确求和”而不是”对 ⟦INLMATH75MATHEND⟧ 采样求期望”这个语境下的另一种呈现，殊途同归。</p>

<p><strong>Top-k</strong>（⟦INLMATH76MATHEND⟧ 是严格子集）时，⟦INLMATH77MATHEND⟧ 且随 ⟦INLMATH78MATHEND⟧ 变化，两个差异项都不为 0——两种写法不再等价。</p>

<p><strong>这个差异往哪个方向推？</strong> 把 ⟦INLMATH79MATHEND⟧ 当成要最小化的目标（让学生逼近老师），梯度下降沿 ⟦INLMATH80MATHEND⟧ 走，其中 ⟦INLMATH81MATHEND⟧ 这一项在优化里的效果是<strong>压低 ⟦INLMATH82MATHEND⟧</strong>——也就是把学生的概率质量推出 ⟦INLMATH83MATHEND⟧ 之外。这不是猜测，是可以直接算出来看到的一条”作弊捷径”：⟦INLMATH84MATHEND⟧ 是按 ⟦INLMATH85MATHEND⟧ 加权、只在 ⟦INLMATH86MATHEND⟧ 上求和的，⟦INLMATH87MATHEND⟧ 外的 token 完全不参与这个求和，所以只要把概率往 ⟦INLMATH88MATHEND⟧ 外面搬，⟦INLMATH89MATHEND⟧ 内每个 ⟦INLMATH90MATHEND⟧ 都会跟着变小，求和值跟着下降——<strong>不需要学生真的更像老师，loss 数值就能降</strong>。我做了个数值实验验证这条捷径确实存在：人为把 ⟦INLMATH91MATHEND⟧ 内 token 的 logit 统一调低一点（概率质量往 ⟦INLMATH92MATHEND⟧ 外挪，⟦INLMATH93MATHEND⟧ 从 0.863 降到 0.793），⟦INLMATH94MATHEND⟧ 从 0.231 直接掉到 0.145——纯粹靠”藏概率”，跟”逼近老师”这个目标毫无关系。</p>

<p>而 in-Reward 的写法因为用的是<strong>归一化</strong>过的 ⟦INLMATH95MATHEND⟧，⟦INLMATH96MATHEND⟧ 内部的”相对占比”和 ⟦INLMATH97MATHEND⟧ 这个”总闸门”被解耦了——advantage 只关心 ⟦INLMATH98MATHEND⟧ 内部谁该多谁该少，不关心 ⟦INLMATH99MATHEND⟧ 整体应该开多大，所以这条捷径天然被堵死。这跟 §2 六宫格的结论完全对得上：<strong>in-Loss 天然容易在意想不到的地方开条缝让优化钻空子，in-Reward（因为 advantage 被 stop-grad）反而更稳。</strong></p>

<p>这也把这一节和之前讨论过的”反向 KL 的稠密截断该按谁的 top-k 做”接了起来：如果 ⟦INLMATH100MATHEND⟧ 选的是教师的 top-k，而学生自己的高概率区域跟教师重叠不多（训练早期的典型情况），⟦INLMATH101MATHEND⟧ 本来就会偏小——这时候如果又叠加 in-Loss 写法，”把概率推向 ⟦INLMATH102MATHEND⟧ 外”这条捷径会格外好走，因为学生本来就有大量概率在 ⟦INLMATH103MATHEND⟧ 外面。<strong>选错 ⟦INLMATH104MATHEND⟧（该用学生的 top-k 却用了教师的）和选错放置位置（该用 in-Reward 却用了 in-Loss），这两个坑叠在一起是最危险的组合。</strong></p>

<p>最后把 OPD 实际的单点采样方式也安放进这个框架：它相当于 ⟦INLMATH105MATHEND⟧、⟦INLMATH106MATHEND⟧（只含采样到的那一个 token）、不做归一化、只走 Adv 通道的极限情形——此时 ⟦INLMATH107MATHEND⟧，”in-Loss 与 in-Reward 不等价”这条结论依然成立，但因为 OPD 本来就走 Adv 通道（第二节已经证明这是精确无偏的格子），这个坑天然被绕开了。</p>

<hr />

<h2 id="五落地清单">五、落地清单</h2>

<p>把这篇的结论收成几条可执行的检查项：</p>

<ol>
  <li><strong>确认你的 reverse KL 走的是 reward 通道，不是 loss 通道。</strong> 如果你想脱离现成的 RL 脚手架自己实现 OPD，一定要保证采样、以及塞进 advantage 的 KL 值都是 <code class="language-plaintext highlighter-rouge">no_grad</code> 的，只让重要性采样比值 ⟦INLMATH108MATHEND⟧ 里的 ⟦INLMATH109MATHEND⟧ 反传。直接对 log-ratio 反传当 loss，梯度期望恒为 0（§2.2）。</li>
  <li><strong>别在 OPD 里为了降方差把 k1 换成 k3。</strong> OPD 的目标就是把学生逼近老师，这正是 k3-in-Reward 梯度塌陷最严重的区间（§2.3）。方差问题用标准 RL 的方差缩减手段解决，不要换估计器。</li>
  <li><strong>Discount factor 按你要不要 Part 2 级联效应来定，不要照抄别的场景的默认值。</strong> KL 正则化想要级联（reward-to-go 自动带上），OPD 不想要（⟦INLMATH110MATHEND⟧，独立评估每一步）——想清楚你的训练信号该有的语义再定这个超参（§3）。</li>
  <li><strong>教师只暴露 top-k logprobs 时，优先为落在 top-k 之外的采样 token 单独查询精确 logprob。</strong> 如果接口不支持，用尾部质量平摊而不是 clip 到第 k 名——后者的低估程度和真实犯错程度成反比，越离谱的错误越被轻判（§4.2、§4.3）。</li>
  <li><strong>top-k 内部的 renormalize 偏移是个常数，通常不影响训练方向，但会让你的 KL 监控数值系统性偏低——不要拿它去做跨 run、跨 ⟦INLMATH111MATHEND⟧ 值的绝对量级对比。</strong></li>
  <li><strong>逐点采样式 reverse KL 和 logit distillation 是两种不同的偏差机制</strong>：前者是”个别点缺失数据”，后者是”整条分布被系统性削尖”——排查问题时先分清楚你用的是哪一种，别用错误的模型去猜错误的成因（§4.4）。</li>
</ol>

<hr />

<h2 id="附录数值验证代码">附录：数值验证代码</h2>

<p>第四节的 top-k 数值实验：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">seed</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="n">V</span><span class="p">,</span> <span class="n">K</span> <span class="o">=</span> <span class="mi">2000</span><span class="p">,</span> <span class="mi">20</span>
<span class="n">logits</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">V</span><span class="p">)</span> <span class="o">*</span> <span class="mf">1.3</span>
<span class="n">logits</span><span class="p">[:</span><span class="mi">5</span><span class="p">]</span> <span class="o">+=</span> <span class="mf">4.0</span>
<span class="n">p_teacher</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logits</span> <span class="o">-</span> <span class="n">logits</span><span class="p">.</span><span class="nf">max</span><span class="p">())</span>
<span class="n">p_teacher</span> <span class="o">/=</span> <span class="n">p_teacher</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>

<span class="n">order</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argsort</span><span class="p">(</span><span class="o">-</span><span class="n">p_teacher</span><span class="p">)</span>
<span class="n">topk_idx</span> <span class="o">=</span> <span class="n">order</span><span class="p">[:</span><span class="n">K</span><span class="p">]</span>
<span class="n">topk_mass</span> <span class="o">=</span> <span class="n">p_teacher</span><span class="p">[</span><span class="n">topk_idx</span><span class="p">].</span><span class="nf">sum</span><span class="p">()</span>
<span class="n">p_topk_renorm</span> <span class="o">=</span> <span class="n">p_teacher</span><span class="p">[</span><span class="n">topk_idx</span><span class="p">]</span> <span class="o">/</span> <span class="n">topk_mass</span>   <span class="c1"># renormalize 到 top-k 内部
</span>
<span class="c1"># top-k 内部：renorm 偏移是常数 -log(topk_mass)
</span><span class="k">for</span> <span class="n">rank</span> <span class="ow">in</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">19</span><span class="p">]:</span>
    <span class="n">tok</span> <span class="o">=</span> <span class="n">topk_idx</span><span class="p">[</span><span class="n">rank</span><span class="p">]</span>
    <span class="n">true_lp</span><span class="p">,</span> <span class="n">renorm_lp</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p_teacher</span><span class="p">[</span><span class="n">tok</span><span class="p">]),</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p_topk_renorm</span><span class="p">[</span><span class="n">rank</span><span class="p">])</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">rank</span><span class="o">+</span><span class="mi">1</span><span class="p">,</span> <span class="n">true_lp</span><span class="p">,</span> <span class="n">renorm_lp</span><span class="p">,</span> <span class="n">renorm_lp</span> <span class="o">-</span> <span class="n">true_lp</span><span class="p">)</span>   <span class="c1"># 恒等于 -log(topk_mass)
</span>
<span class="c1"># top-k 外部：clip 到第 k 名，低估程度随真实概率越低而越严重
</span><span class="n">worst</span> <span class="o">=</span> <span class="n">order</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
<span class="nf">print</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p_teacher</span><span class="p">[</span><span class="n">worst</span><span class="p">])</span> <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p_teacher</span><span class="p">[</span><span class="n">topk_idx</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]]))</span>   <span class="c1"># 越负，低估越狠
</span></code></pre></div></div>

<p>运行结果见第四节正文。核心数字：top-k 内部的偏移恒为常数（⟦INLMATH112MATHEND⟧，这里是 ⟦INLMATH113MATHEND⟧ nat）；top-k 外部用 clip 兜底时，学生犯的错越大，低估越狠——例子里差了 7 个 nat，对应概率空间三个数量级。</p>

<p>第 4.5 节”⟦INLMATH114MATHEND⟧ 尺度因子”恒等式的验证：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">seed</span><span class="p">(</span><span class="mi">7</span><span class="p">)</span>
<span class="n">V</span> <span class="o">=</span> <span class="mi">30</span>
<span class="k">def</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">):</span>
    <span class="n">z</span> <span class="o">=</span> <span class="n">z</span> <span class="o">-</span> <span class="n">z</span><span class="p">.</span><span class="nf">max</span><span class="p">();</span> <span class="n">e</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">z</span><span class="p">);</span> <span class="k">return</span> <span class="n">e</span><span class="o">/</span><span class="n">e</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>

<span class="n">z_teacher</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">V</span><span class="p">)</span><span class="o">*</span><span class="mf">1.5</span>
<span class="n">z_theta</span> <span class="o">=</span> <span class="n">z_teacher</span> <span class="o">+</span> <span class="mf">0.6</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">V</span><span class="p">)</span>
<span class="n">pi_teacher</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z_teacher</span><span class="p">)</span>
<span class="n">K</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argsort</span><span class="p">(</span><span class="o">-</span><span class="nf">softmax</span><span class="p">(</span><span class="n">z_theta</span><span class="p">))[:</span><span class="mi">8</span><span class="p">]</span>   <span class="c1"># 任选一种 top-k，结论跟 K 是谁的 top-k 无关
</span>
<span class="k">def</span> <span class="nf">k1_of</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">):</span>
    <span class="n">p</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p</span><span class="p">[</span><span class="n">K</span><span class="p">])</span> <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">pi_teacher</span><span class="p">[</span><span class="n">K</span><span class="p">])</span>

<span class="k">def</span> <span class="nf">L_loss</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">):</span>                       <span class="c1"># \hat D_K^Loss：截断求和，不归一化
</span>    <span class="n">p</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">p</span><span class="p">[</span><span class="n">K</span><span class="p">]</span> <span class="o">*</span> <span class="nf">k1_of</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">)).</span><span class="nf">sum</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">Z_of</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">):</span>
    <span class="k">return</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">)[</span><span class="n">K</span><span class="p">].</span><span class="nf">sum</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">grad_Ladv_analytic</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">):</span>           <span class="c1"># 纯得分函数项：sum piK(v) * k1(v) * grad log pi(v)
</span>    <span class="n">p</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="n">Z</span> <span class="o">=</span> <span class="n">p</span><span class="p">[</span><span class="n">K</span><span class="p">].</span><span class="nf">sum</span><span class="p">()</span>
    <span class="n">piK</span> <span class="o">=</span> <span class="n">p</span><span class="p">[</span><span class="n">K</span><span class="p">]</span> <span class="o">/</span> <span class="n">Z</span>
    <span class="n">k1v</span> <span class="o">=</span> <span class="nf">k1_of</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>
    <span class="n">G</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">V</span><span class="p">)</span> <span class="o">-</span> <span class="n">p</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>          <span class="c1"># G[v] = grad_z log pi(v)
</span>    <span class="k">return</span> <span class="nf">sum</span><span class="p">(</span><span class="n">piK</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">*</span><span class="n">k1v</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">*</span><span class="n">G</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">v</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">K</span><span class="p">))</span>

<span class="n">h</span> <span class="o">=</span> <span class="mf">1e-6</span>
<span class="n">grad_Lloss_numeric</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span>
    <span class="p">(</span><span class="nc">L_loss</span><span class="p">(</span><span class="n">z_theta</span> <span class="o">+</span> <span class="n">h</span><span class="o">*</span><span class="n">e</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span> <span class="o">-</span> <span class="nc">L_loss</span><span class="p">(</span><span class="n">z_theta</span> <span class="o">-</span> <span class="n">h</span><span class="o">*</span><span class="n">e</span><span class="p">,</span> <span class="n">K</span><span class="p">))</span> <span class="o">/</span> <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="n">h</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">V</span><span class="p">)])</span>
<span class="n">grad_Z_numeric</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span>
    <span class="p">(</span><span class="nc">Z_of</span><span class="p">(</span><span class="n">z_theta</span> <span class="o">+</span> <span class="n">h</span><span class="o">*</span><span class="n">e</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span> <span class="o">-</span> <span class="nc">Z_of</span><span class="p">(</span><span class="n">z_theta</span> <span class="o">-</span> <span class="n">h</span><span class="o">*</span><span class="n">e</span><span class="p">,</span> <span class="n">K</span><span class="p">))</span> <span class="o">/</span> <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="n">h</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">V</span><span class="p">)])</span>

<span class="n">Z</span> <span class="o">=</span> <span class="nc">Z_of</span><span class="p">(</span><span class="n">z_theta</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>
<span class="n">grad_Ladv</span> <span class="o">=</span> <span class="nf">grad_Ladv_analytic</span><span class="p">(</span><span class="n">z_theta</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">||grad_Lloss - (Z*grad_Ladv + grad_Z)|| =</span><span class="sh">"</span><span class="p">,</span>
      <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">grad_Lloss_numeric</span> <span class="o">-</span> <span class="p">(</span><span class="n">Z</span><span class="o">*</span><span class="n">grad_Ladv</span> <span class="o">+</span> <span class="n">grad_Z_numeric</span><span class="p">)))</span>   <span class="c1"># ~1e-10，恒等式成立
</span><span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">||grad_Lloss - (grad_Ladv + grad_Z)||   =</span><span class="sh">"</span><span class="p">,</span>
      <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">grad_Lloss_numeric</span> <span class="o">-</span> <span class="p">(</span><span class="n">grad_Ladv</span> <span class="o">+</span> <span class="n">grad_Z_numeric</span><span class="p">)))</span>    <span class="c1"># ~0.036，没有 Z 尺度因子的版本不成立
</span>
<span class="c1"># loss-hack 方向验证：把 K 内 logits 统一调低，概率质量往 K 外挪，L_loss 是否会无关地下降
</span><span class="n">z_pushed</span> <span class="o">=</span> <span class="n">z_theta</span><span class="p">.</span><span class="nf">copy</span><span class="p">();</span> <span class="n">z_pushed</span><span class="p">[</span><span class="n">K</span><span class="p">]</span> <span class="o">-=</span> <span class="mf">0.5</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Z(K) 从</span><span class="sh">"</span><span class="p">,</span> <span class="n">Z</span><span class="p">,</span> <span class="sh">"</span><span class="s">变成</span><span class="sh">"</span><span class="p">,</span> <span class="nc">Z_of</span><span class="p">(</span><span class="n">z_pushed</span><span class="p">,</span> <span class="n">K</span><span class="p">))</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">L_loss  从</span><span class="sh">"</span><span class="p">,</span> <span class="nc">L_loss</span><span class="p">(</span><span class="n">z_theta</span><span class="p">,</span> <span class="n">K</span><span class="p">),</span> <span class="sh">"</span><span class="s">变成</span><span class="sh">"</span><span class="p">,</span> <span class="nc">L_loss</span><span class="p">(</span><span class="n">z_pushed</span><span class="p">,</span> <span class="n">K</span><span class="p">))</span>
</code></pre></div></div>

<p>数值结果：带 ⟦INLMATH115MATHEND⟧ 尺度因子的关系式残差 ⟦INLMATH116MATHEND⟧（精确恒等式）；不带尺度因子的简化版本残差 ⟦INLMATH117MATHEND⟧（不成立，说明这个尺度因子是必要的，不能省略）。loss-hack 方向验证：⟦INLMATH118MATHEND⟧ 从 0.863 降到 0.793 时，⟦INLMATH119MATHEND⟧ 从 0.231 降到 0.145——纯粹靠往 ⟦INLMATH120MATHEND⟧ 外藏概率，loss 数值就会下降。</p>

<h2 id="参考">参考</h2>

<ol>
  <li><a href="https://thinkingmachines.ai/blog/on-policy-distillation/">On-Policy Distillation</a> —— Thinking Machines Lab，OPD 的原始博客，本文第一、三节的事实性描述均来自这里。</li>
  <li><a href="/reinforcement-learning/2026/09/02/kl-divergence-correct-form.html">KL散度的正确形式是什么</a> —— 本文的姊妹篇，六宫格、k1/k2/k3 的无偏性/方差分析、in-Reward/in-Loss 的梯度证明全部来自这篇，本文第二节全程复用其结论。</li>
  <li><a href="https://zhuanlan.zhihu.com/p/2068017517788927592">OPD 实现细节拆解：KL 放在 Loss，还是放在 Advantage？</a> —— 第 4.5 节 top-k 场景下 in-Loss/in-Adv 梯度对比的问题framing来自这篇；本文用自己的符号独立重新推导了梯度关系式，并发现共享项之间还有一个 ⟦INLMATH121MATHEND⟧ 尺度因子（原文的简化表述没有包含这一项，数值验证见附录），同时补充了 loss-hack 机制的数值验证。</li>
</ol>]]></content><author><name></name></author><category term="reinforcement-learning" /><summary type="html"><![CDATA[On-Policy Distillation 里的 reverse KL 恰好落在六宫格中"永远对"的 k1-in-Reward 格子；本文接着分析换成 k3、discount 设为 0、以及教师只暴露 top-k logprobs 这几个工程现实分别会踩什么坑。]]></summary></entry><entry xml:lang="zh"><title type="html">LLM 强化学习中 KL 散度的正确形式是什么</title><link href="https://hbell99.github.io/reinforcement-learning/2026/09/02/kl-divergence-correct-form.html" rel="alternate" type="text/html" title="LLM 强化学习中 KL 散度的正确形式是什么" /><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://hbell99.github.io/reinforcement-learning/2026/09/02/kl-divergence-correct-form</id><content type="html" xml:base="https://hbell99.github.io/reinforcement-learning/2026/09/02/kl-divergence-correct-form.html"><![CDATA[<p>我们熟知LLM里有3种典型的KL估计器，也经常听到说这个估计器是无偏的，那个估计器是有偏的；又或者听到说这个估计器虽然是无偏的但是梯度估计是有偏的。
不熟悉的人可能会问，什么叫无偏？什么叫有偏？梯度无偏有偏又是怎么回事？</p>

<p>首先，先介绍一下怎么判断”无偏”还是”有偏”？
<strong>判断标准只有一个：看数学期望（平均值）是否等于目标值。</strong></p>
<ul>
  <li><strong>无偏（Unbiased）</strong>：如果你重复无数次实验，计算所有估计值的平均值，这个平均值 <strong>恰好等于</strong> 你要估计的那个真实值。</li>
  <li><strong>有偏（Biased）</strong>：这个平均值 <strong>不等于</strong> 真实值，存在系统性偏差。</li>
</ul>

<p>对于KL无偏估计器，意味着⟦INLMATH29MATHEND⟧；对于梯度无偏估计器，意味着⟦INLMATH30MATHEND⟧。</p>

<p>这里其实没有什么特别神秘的地方。<strong>区别只在于：你到底在估计什么。</strong></p>

<p>而这句话正是整篇文章的题眼。因为在 RL 训练里，我们其实同时在估计<strong>两个不同的东西</strong>：</p>

<ol>
  <li><strong>KL 的值</strong>——它进 log、进监控面板、进 early stop 的判据，我们希望它准；</li>
  <li><strong>KL 的梯度</strong>——它才是真正改变模型参数的东西，我们希望它指向正确的方向。</li>
</ol>

<p>这两件事<strong>没有任何逻辑蕴含关系</strong>。一个估计器可以值无偏而梯度有偏，也可以值有偏而梯度无偏。</p>

<p>下面把这件事拆开讲。</p>

<hr />

<h2 id="一三个估计器k1--k2--k3">一、三个估计器：k1 / k2 / k3</h2>

<p>先把符号定好。我们要估计的是当前 policy ⟦INLMATH31MATHEND⟧ 相对参考模型 ⟦INLMATH32MATHEND⟧ 的 KL：</p>

<p>⟦DISPMATH1MATHEND⟧</p>

<p>这里顺手把方向的名字定下来，后面会反复用到：⟦INLMATH33MATHEND⟧ 叫<strong>反向 KL（reverse KL）</strong>——它是 mode-seeking 的，⟦INLMATH34MATHEND⟧ 只需要在自己有输出的地方跟 ref 对齐，ref 有但 ⟦INLMATH35MATHEND⟧ 没生成过的地方完全不管。反过来 ⟦INLMATH36MATHEND⟧ 叫<strong>正向 KL（forward KL）</strong>，是 mass-covering 的——它要求 ⟦INLMATH37MATHEND⟧ 在 ref 有质量的地方都不能塌成 0。</p>

<p>RLHF 用反向 KL 不是随手选的：一是它只需要对 ⟦INLMATH38MATHEND⟧ 采样（不用枚举 ref 的支撑集），工程上天然契合”用当前策略生成、算 log-ratio”这套流程；二是它是 zero-forcing 的——只要 ⟦INLMATH39MATHEND⟧ 敢生成一个 ref 认为极不可能的东西，KL 就会爆炸，这正好是”别让模型说出 ref 绝对不会说的话”这个防 reward hacking 的诉求。本文第一节到第五节说的”KL”，如果不特别说明，都是指反向 KL ⟦INLMATH40MATHEND⟧。</p>

<p>注意这是一个<strong>期望</strong>，而我们只有采样。为了记号方便，定义似然比</p>

<p>⟦DISPMATH2MATHEND⟧</p>

<p>（注意方向：分子是 ref。这个方向的好处是 ⟦INLMATH41MATHEND⟧，后面反复要用。）</p>

<h3 id="k1最朴素的那个">k1：最朴素的那个</h3>

<p>⟦DISPMATH3MATHEND⟧</p>

<p>直接把期望里的东西拿出来当估计。<strong>它显然是无偏的</strong>——这几乎是同义反复，因为 KL 的定义就是它的期望。</p>

<p>但 k1 有个很扎眼的毛病：<strong>KL 恒非负，而 k1 有一半的取值空间是负数</strong>。你采一条 response，很可能算出个负的”KL”。这不是 bug，是单样本估计的本性，但它意味着<strong>方差极大</strong>。</p>

<p>这个方差有多大？我做了个数值实验（50 类离散分布，附录有脚本）：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>真实 KL</th>
      <th>k1 的标准差</th>
      <th>标准差 / 信号</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0.1921</td>
      <td>0.6435</td>
      <td>3.3×</td>
    </tr>
    <tr>
      <td>0.0197</td>
      <td>0.2001</td>
      <td>10×</td>
    </tr>
    <tr>
      <td>0.0011</td>
      <td>0.0476</td>
      <td>42×</td>
    </tr>
    <tr>
      <td>0.00018</td>
      <td>0.0190</td>
      <td><strong>105×</strong></td>
    </tr>
  </tbody>
</table></div>

<p>结论很清楚：<strong>⟦INLMATH42MATHEND⟧ 越接近 ⟦INLMATH43MATHEND⟧，k1 的信噪比越差</strong>，而”接近 ref”恰恰是 RL 训练绝大部分时间所处的状态。所以如果用 k1 单样本值去画监控曲线——画出来的可能基本是噪声。</p>

<p>（顺带一提：这个”信噪比随 KL 变小而变差”的规律，本身就说明了 k2/k3 这类方差缩减手段的必要性不是可有可无的工程优化，而是刚需。）</p>

<h3 id="k2把它掰成非负的">k2：把它掰成非负的</h3>

<p>⟦DISPMATH4MATHEND⟧</p>

<p>平方一下就非负了。代价是<strong>它对 KL 有偏</strong>。</p>

<p>但这个偏有多大？把 k1 和 k2 在 ⟦INLMATH44MATHEND⟧ 附近做二阶 Taylor 展开：</p>

<p>⟦DISPMATH5MATHEND⟧</p>

<p>两者只差一个线性项 ⟦INLMATH45MATHEND⟧，而这一项的期望恰好是 0（因为 ⟦INLMATH46MATHEND⟧）。所以 <strong>k2 的偏差是三阶小量</strong>——当 ⟦INLMATH47MATHEND⟧ 时可以忽略。数值上：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>真实 KL</th>
      <th>E[k2]</th>
      <th>相对偏差</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0.1921</td>
      <td>0.2255</td>
      <td>+17%</td>
    </tr>
    <tr>
      <td>0.0197</td>
      <td>0.0202</td>
      <td>+2.4%</td>
    </tr>
    <tr>
      <td>0.0011</td>
      <td>0.00113</td>
      <td>&lt;0.5%</td>
    </tr>
  </tbody>
</table></div>

<p>所以 k2 的定位很清楚：<strong>近距离下的低方差近似</strong>。KL 大到 0.2 那个量级时，它会系统性高估 17%，这时候你不该再信它的绝对值。</p>

<h3 id="k3控制变量法">k3：控制变量法</h3>

<p>k3 的思路更漂亮一点。既然 k1 无偏但方差大，那就给它<strong>加一个期望为 0 的项</strong>来降方差——这是统计里标准的控制变量（control variate）技巧：</p>

<p>⟦DISPMATH6MATHEND⟧</p>

<p>⟦INLMATH48MATHEND⟧，所以<strong>无论 ⟦INLMATH49MATHEND⟧ 取多少，k3 都是 KL 的无偏估计</strong>。而只要 ⟦INLMATH50MATHEND⟧ 和 ⟦INLMATH51MATHEND⟧ 负相关，就能降方差。</p>

<p>取 ⟦INLMATH52MATHEND⟧，理由不是方差最优，而是<strong>保证非负</strong>：由 ⟦INLMATH53MATHEND⟧ 可得 ⟦INLMATH54MATHEND⟧。</p>

<p>这里有个细节值得提：⟦INLMATH55MATHEND⟧ 到底离方差最优有多远？理论上最优 ⟦INLMATH56MATHEND⟧。我数值算了一下：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>真实 KL</th>
      <th>⟦INLMATH57MATHEND⟧</th>
      <th>sd(k3, λ=1)</th>
      <th>sd(k3, λ*)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0.1921</td>
      <td>0.846</td>
      <td>0.3270</td>
      <td>0.3105</td>
    </tr>
    <tr>
      <td>0.0197</td>
      <td>1.015</td>
      <td>0.01846</td>
      <td>0.01823</td>
    </tr>
    <tr>
      <td>0.00018</td>
      <td>0.995</td>
      <td>0.000300</td>
      <td>0.000280</td>
    </tr>
  </tbody>
</table></div>

<p><strong>当两个分布接近时 ⟦INLMATH58MATHEND⟧</strong>，也就是说 Schulman 为了非负性选的 ⟦INLMATH59MATHEND⟧ 恰好几乎就是方差最优的。这不是巧合——在 ⟦INLMATH60MATHEND⟧ 的极限下这两个诉求会合流。这大概是 k3 之所以好用的深层原因：它同时满足了非负、无偏、近似方差最优三件事。</p>

<h3 id="小结这一节">小结这一节</h3>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th> </th>
      <th>对 KL 值</th>
      <th>非负</th>
      <th>方差</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>k1</td>
      <td><strong>无偏</strong></td>
      <td>否</td>
      <td>很大</td>
    </tr>
    <tr>
      <td>k2</td>
      <td>有偏（三阶小）</td>
      <td>是</td>
      <td>小</td>
    </tr>
    <tr>
      <td>k3</td>
      <td><strong>无偏</strong></td>
      <td>是（λ=1）</td>
      <td>小，近似最优</td>
    </tr>
  </tbody>
</table></div>

<p>看到这张表，任何人的第一反应都是：<strong>k3 全面占优，用 k3 就完了</strong>。事实上业界也是这么做的——很多框架的 GRPO 干脆只实现了 k3。</p>

<p><strong>但这张表只回答了”值”的问题。一旦进入梯度，结论会整个翻过来。</strong></p>

<hr />

<h2 id="二唯一需要记住的那个公式梯度有两项">二、唯一需要记住的那个公式：梯度有两项</h2>

<p>这是整篇文章的技术核心，理解了它，后面所有结论都是推论。</p>

<p>把 KL 估计项统一记作 ⟦INLMATH61MATHEND⟧（k1/k2/k3 都可以代进去）。注意 <strong>⟦INLMATH62MATHEND⟧ 的下标 ⟦INLMATH63MATHEND⟧ 和采样分布的 ⟦INLMATH64MATHEND⟧ 是同一个 ⟦INLMATH65MATHEND⟧</strong>——这是全部麻烦的来源。</p>

<p>⟦DISPMATH7MATHEND⟧</p>

<p>求和号里<strong>有两处依赖 ⟦INLMATH66MATHEND⟧</strong>。所以求导要用乘积法则：</p>

<p>⟦DISPMATH8MATHEND⟧</p>

<p>（第二个等号用了对数技巧 ⟦INLMATH67MATHEND⟧。）</p>

<p>这两项的物理含义完全不同，值得掰开说：</p>

<ul>
  <li><strong>得分函数项</strong>：⟦INLMATH68MATHEND⟧ 变了 → <strong>采到的 ⟦INLMATH69MATHEND⟧ 的分布变了</strong> → KL 变了。这是”我少生成一点 KL 高的序列”。</li>
  <li><strong>路径导数项</strong>：⟦INLMATH70MATHEND⟧ 变了 → <strong>同一个 ⟦INLMATH71MATHEND⟧ 上 KL 的数值变了</strong> → KL 变了。这是”对于我已经生成的这条序列，我把它的 log prob 往 ref 拉”。</li>
</ul>

<p><strong>真正的 KL 梯度必须两项都有。</strong> 这就是”正确形式”。</p>

<p>而现实是——<strong>没有任何主流实现两项都算</strong>。所有实现都只算其中一项。哪一项被丢掉，取决于你在哪里 stop-gradient。</p>

<hr />

<h2 id="三两种放法其实是两种-stop-gradient">三、两种放法，其实是两种 stop-gradient</h2>

<p>加了 KL 正则的目标函数，<strong>正确</strong>的写法是（用红色标记需要回传梯度的 ⟦INLMATH72MATHEND⟧）：</p>

<p>⟦DISPMATH9MATHEND⟧</p>

<p>注意 KL 项里<strong>采样分布的 ⟦INLMATH73MATHEND⟧ 和 log 里的 ⟦INLMATH74MATHEND⟧ 都要回传梯度</strong>。</p>

<p>但工程上，实际跑的永远是下面两个变体之一：</p>

<p><strong>KL in Reward（IR，OpenAI/InstructGPT 路线）</strong></p>

<p>⟦DISPMATH10MATHEND⟧</p>

<p>log 项里的 ⟦INLMATH75MATHEND⟧ <strong>没有</strong>标红——它被 stop-grad 了。实现上就是把 KL 当成一个数值塞进 reward：</p>

<p>⟦DISPMATH11MATHEND⟧</p>

<p><code class="language-plaintext highlighter-rouge">trl</code> 的 PPO 里这段整个包在 <code class="language-plaintext highlighter-rouge">torch.no_grad()</code> 里，非常直白：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="n">logr</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">logprobs</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="o">-</span><span class="n">logr</span> <span class="k">if</span> <span class="n">args</span><span class="p">.</span><span class="n">kl_estimator</span> <span class="o">==</span> <span class="sh">"</span><span class="s">k1</span><span class="sh">"</span> <span class="nf">else </span><span class="p">(</span><span class="n">logr</span><span class="p">.</span><span class="nf">exp</span><span class="p">()</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-</span> <span class="n">logr</span>   <span class="c1"># else 分支是 k3
</span>    <span class="n">non_score_reward</span> <span class="o">=</span> <span class="o">-</span><span class="n">args</span><span class="p">.</span><span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span>
    <span class="n">rewards</span> <span class="o">=</span> <span class="n">non_score_reward</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
    <span class="n">rewards</span><span class="p">[</span><span class="n">actual_start</span><span class="p">,</span> <span class="n">actual_end</span><span class="p">]</span> <span class="o">+=</span> <span class="n">scores</span>
</code></pre></div></div>

<p><strong>IR = 丢掉路径导数，只保留得分函数导数。</strong></p>

<p><strong>KL in Loss（IL，DeepSeek/GRPO 路线）</strong></p>

<p>⟦DISPMATH12MATHEND⟧</p>

<p>这次是<strong>采样分布</strong>的 ⟦INLMATH76MATHEND⟧ 被 stop-grad 了（实现上体现为：KL 项直接作为一个 loss 加进去，不经过 advantage / policy gradient 通道）。</p>

<p><strong>IL = 丢掉得分函数导数，只保留路径导数。</strong></p>

<p>所以，回到题目里的那个问题——<strong>实现细节（是否 stop-gradient 某一项）会不会改变有偏性结论？</strong></p>

<p><strong>答案是：它就是有偏性结论本身。</strong> IR 和 IL 的全部区别就是 stop-grad 打在哪儿；而 stop-grad 打在哪儿，决定了两项梯度里哪一项被丢。</p>

<hr />

<h2 id="四六宫格3-个估计器--2-种放法">四、六宫格：3 个估计器 × 2 种放法</h2>

<p>现在把 k1/k2/k3 分别代进 IR 和 IL，逐格算。我把结论先摆出来，再讲推导：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th> </th>
      <th><strong>KL in Reward</strong>（丢路径导数）</th>
      <th><strong>KL in Loss</strong>（丢得分函数导数）</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>k1</strong></td>
      <td>✅ <strong>梯度无偏（精确等于真梯度）</strong></td>
      <td>❌ 梯度恒等于 0，纯噪声</td>
    </tr>
    <tr>
      <td><strong>k2</strong></td>
      <td>❌ 正则塌陷</td>
      <td>✅ <strong>梯度无偏（精确等于真梯度）</strong></td>
    </tr>
    <tr>
      <td><strong>k3</strong></td>
      <td>❌ 正则塌陷</td>
      <td>⚠️ 精确的<strong>正向</strong> KL 梯度</td>
    </tr>
  </tbody>
</table></div>

<p>这张表的<strong>反直觉之处</strong>在于：k1 和 k2 的角色是<strong>完全对调</strong>的。值上无偏的 k1，只在 reward 里对；值上有偏的 k2，只在 loss 里对。而值上”全面占优”的 k3，<strong>两边都不是我们想要的反向 KL 梯度</strong>。<code class="language-plaintext highlighter-rouge">verl</code> 甚至专门提供了 k3+ 版本，前向算 k3 的值（非负、低方差，报出去当监控数值），反向传播时却换成 k2 的梯度——本质上就是拿”这一格的问题”当作一个已知坑主动绕开：数值走 k3，梯度走 k2-IL（等于 4.3 那个恒等式），两头都占。</p>

<p>但要强调一点，也是我后来才想明白的：<strong>表里的 ❌ 和 ⚠️ 严重程度差了两个数量级，而且方向相反。</strong></p>

<ul>
  <li>❌ 那几格是<strong>失效</strong>：k1-IL 梯度恒为 0，k2/k3-IR 的梯度在 ⟦INLMATH77MATHEND⟧ 时会塌陷到真梯度的 1% 以下（4.4 会算给你看）。你以为加了正则，其实没加。</li>
  <li>⚠️ 那格是<strong>换了个散度</strong>：k3-IL 不是”算歪了的反向 KL”，它精确地是<strong>正向 KL 的梯度</strong>。而正向 KL 本身就是个合法且好用的正则化目标（只是 mass-covering，跟我们设计初心的 mode-seeking 方向不同）。</li>
</ul>

<p>这个区分很重要，因为它解释了一个否则很难理解的现象：<strong>既然 k3 两边都不是真梯度，为什么业界还是普遍在用它？</strong> 答案是大家用的是 ⚠️ 那格（GRPO 系走的都是 IL），而那一格实践上站得住。真正的坑是 ❌ 那几格。</p>

<h3 id="41-为什么-k1-in-reward-是精确无偏的">4.1 为什么 k1-in-Reward 是精确无偏的</h3>

<p>IR 丢掉了路径导数，看起来一定错。但对 k1：</p>

<p>⟦DISPMATH13MATHEND⟧</p>

<p>（⟦INLMATH78MATHEND⟧ 与 ⟦INLMATH79MATHEND⟧ 无关，直接消失。）而这一项的期望<strong>恒为 0</strong>：</p>

<p>⟦DISPMATH14MATHEND⟧</p>

<p><strong>丢掉的东西期望为 0，所以什么都没丢。</strong> k1-IR 的梯度和完整正确梯度<strong>完全一致</strong>，不是近似，是恒等。</p>

<p>这带来一个很有用的推论：对 k1 而言，log 项里的 ⟦INLMATH80MATHEND⟧ 要不要 stop-grad，<strong>梯度形式一模一样</strong>：</p>

<p>⟦DISPMATH15MATHEND⟧</p>

<p><strong>k1 对 stop-grad 免疫。</strong> 这个鲁棒性本身就是一个很强的工程优点——它意味着这条路径上少一个可以写错的地方。</p>

<h3 id="42-为什么-k1-in-loss-是纯噪声">4.2 为什么 k1-in-Loss 是纯噪声</h3>

<p>同一个事实，换个位置，结论就反过来了。IL 保留的<strong>恰好就是</strong>那个期望为 0 的路径导数项：</p>

<p>⟦DISPMATH16MATHEND⟧</p>

<p><strong>k1-in-Loss 的梯度期望恒为 0。</strong> 它不是”有偏”——它是<strong>根本没有正则化作用</strong>。你以为你在约束模型不要偏离 ref，实际上你只是往梯度里注入了一坨均值为 0 的噪声。它唯一的效果是增大梯度方差。</p>

<p>这大概是六宫格里最危险的一格：<strong>它不报错，loss 曲线看起来也正常，但它什么都没做。</strong></p>

<h3 id="43-为什么-k2-in-loss-反而是对的">4.3 为什么 k2-in-Loss 反而是对的</h3>

<p>k2 的路径导数：</p>

<p>⟦DISPMATH17MATHEND⟧</p>

<p>对比 4.1 的结果——<strong>这和 k1-IR 的梯度一字不差</strong>。</p>

<p>⟦DISPMATH18MATHEND⟧</p>

<p>这个恒等式非常值得记住。它说明：k2 那个”多出来”的平方，作用不是改变 KL 的值，而是<strong>把 KL 的值当成系数塞进路径导数里，从而人工复现出得分函数项</strong>。<strong>k2 在值上的有偏，换来了梯度上的精确无偏。</strong></p>

<p>也就是说，<strong>“k2 是有偏估计器”这句话，在梯度语境下是彻底的误导。</strong></p>

<h3 id="44-k3-两边都不是真梯度但两边错得完全不一样">4.4 k3 两边都不是真梯度——但两边错得完全不一样</h3>

<p>k3（λ=1）的路径导数：</p>

<p>⟦DISPMATH19MATHEND⟧</p>

<p>对比 k2-IL（正确）的系数 ⟦INLMATH81MATHEND⟧，k3-IL 的系数是 ⟦INLMATH82MATHEND⟧。两者在 ⟦INLMATH83MATHEND⟧ 时一致，但在远处会分道扬镳：</p>

<ul>
  <li>⟦INLMATH84MATHEND⟧（⟦INLMATH85MATHEND⟧）：正确系数 ⟦INLMATH86MATHEND⟧，强烈惩罚；k3 的系数只趋于 ⟦INLMATH87MATHEND⟧，<strong>惩罚不足</strong>。</li>
  <li>⟦INLMATH88MATHEND⟧（⟦INLMATH89MATHEND⟧）：正确系数以 ⟦INLMATH90MATHEND⟧ 增长，k3 的系数<strong>线性增长</strong>，更激进。</li>
</ul>

<p>不过，把这些系数差异说成”k3-IL 是个粗糙近似”其实没抓到点子上。上面那个期望有一个精确的身份。注意 ⟦INLMATH91MATHEND⟧，所以</p>

<p>⟦DISPMATH20MATHEND⟧</p>

<p>也就是：</p>

<p>⟦DISPMATH21MATHEND⟧</p>

<p>（数值验证残差 ⟦INLMATH92MATHEND⟧，见附录 B。）</p>

<p><strong>k3-in-Loss 不是”算歪了的反向 KL 梯度”，它是精确的正向 KL 梯度。</strong> 这是个完全不同性质的陈述——它不是在近似一个东西而近似歪了，它是在<strong>精确地优化另一个东西</strong>。</p>

<p>这也是 GRPO 原始论文（DeepSeekMath）里那个 k3 loss 项真正的问题所在：写下 ⟦INLMATH93MATHEND⟧ 直接反传的人，本意显然是要用反向 KL 拴住模型（这是全文开头就讲清楚的设计初心），但代码算出来的却是正向 KL 的梯度——<strong>不是大小错了，是方向错了</strong>。第五节会看到，这个”方向错误”的根源和”能不能补救”，其实跟采样是不是 on-policy 密切相关，第六节会专门展开。</p>

<p>所以 k3-IL 至少在 on-policy、⟦INLMATH94MATHEND⟧ 的区间里是个<strong>可用</strong>的选择，只是你应该知道自己在正则哪个散度。它跟 k1-IL（梯度恒为 0，什么都没做）完全不是一个错误等级。</p>

<h4 id="真正的坑k3-in-reward">真正的坑：k3-in-Reward</h4>

<p>k3-IR 的梯度是 ⟦INLMATH95MATHEND⟧，比正确梯度 ⟦INLMATH96MATHEND⟧ 多了一项 ⟦INLMATH97MATHEND⟧。而这一项我们刚刚才算过——它正是 ⟦INLMATH98MATHEND⟧。所以（取 ⟦INLMATH99MATHEND⟧）：</p>

<p>⟦DISPMATH22MATHEND⟧</p>

<p>（我数值验证过，误差 ⟦INLMATH100MATHEND⟧，见附录。）</p>

<p>这个式子有两个推论，第二个尤其重要：</p>

<p><strong>推论 1：k3-IR 想给的是反向 KL 的梯度，但额外混入了一个符号相反的正向 KL 梯度项。</strong> 它不是”带噪声的正则化”，噪声是零均值的，这个混入项不是——它是一个系统性的、方向相反的干扰项。</p>

<p><strong>推论 2（更要命的）：在 ⟦INLMATH101MATHEND⟧ 时，这两项会一阶抵消。</strong> 因为在小扰动下正向和反向 KL 都近似等于 ⟦INLMATH102MATHEND⟧（⟦INLMATH103MATHEND⟧ 是 Fisher 信息矩阵），二者到二阶为止是<strong>相等的</strong>，梯度也就相互抵消了。</p>

<p>换个角度看更直观：k3-IR 的梯度系数是 ⟦INLMATH104MATHEND⟧，是<strong>二阶小量</strong>；而正确系数 ⟦INLMATH105MATHEND⟧ 是<strong>一阶小量</strong>。差了整整一阶。</p>

<p>数值上这个效应非常剧烈：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>⟦INLMATH106MATHEND⟧</th>
      <th>真梯度模长</th>
      <th>k3-IR 梯度模长</th>
      <th>比例</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>大</td>
      <td>1.76e-1</td>
      <td>3.04e-2</td>
      <td>17%</td>
    </tr>
    <tr>
      <td>中</td>
      <td>1.01e-1</td>
      <td>2.82e-2</td>
      <td>28%</td>
    </tr>
    <tr>
      <td>小</td>
      <td>1.52e-2</td>
      <td>7.54e-4</td>
      <td>5.0%</td>
    </tr>
    <tr>
      <td>很小</td>
      <td>3.03e-2</td>
      <td>2.25e-4</td>
      <td><strong>0.74%</strong></td>
    </tr>
  </tbody>
</table></div>

<p><strong>所以 k3-in-Reward 的问题不是”有点偏”，而是”在你最需要它工作的区间里，它几乎不产生任何正则化梯度”。</strong></p>

<p>这解释了一个很多人踩过但没想明白的现象：<em>明明加了 KL 惩罚、监控面板上 KL 数值也算得挺准（k3 值确实无偏！），但模型该漂还是漂。</em> 因为值算对了，梯度是空的。</p>

<p>（k2-in-Reward 有完全相同的病理，原因一样：k2 的系数也是二阶小量。看数值表里 k2_IR 的模长和 k3_IR 几乎一样小。）</p>

<h4 id="把-ir-和-il-并排放同一个-07相反的含义">把 IR 和 IL 并排放：同一个 0.7%，相反的含义</h4>

<p>k3 的两格现在都有了精确身份，放在一起看非常有意思：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th> </th>
      <th>精确身份</th>
      <th>⟦INLMATH107MATHEND⟧ 时</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>k3-<strong>IL</strong></td>
      <td>⟦INLMATH108MATHEND⟧（正向 KL 梯度）</td>
      <td>相对真梯度（反向 KL 梯度）误差 → <strong>0.63%</strong>（几乎完美）</td>
    </tr>
    <tr>
      <td>k3-<strong>IR</strong></td>
      <td>⟦INLMATH109MATHEND⟧（反向 − 正向）</td>
      <td>梯度模长 → 真梯度的 <strong>0.74%</strong>（几乎为零）</td>
    </tr>
  </tbody>
</table></div>

<p>两个都是 0.7% 量级的数字，含义却完全相反：<strong>IL 是”只差 0.6%”，IR 是”只剩 0.7%”。</strong></p>

<p>根源就是那个减号。IL 保留的是单个正向 KL 梯度——一个一阶量，恰好在 ⟦INLMATH110MATHEND⟧ 处跟反向 KL 梯度几乎重合；IR 保留的是反向、正向两个 KL 梯度<strong>相减</strong>——一阶部分抵消掉，只剩二阶残渣。</p>

<p><strong>同一个估计器，换个位置，从”几乎完美”变成”几乎为零”。</strong> 这大概是全文最能说明”有偏性是组合的属性、不是估计器的属性”的一个例子。</p>

<hr />

<h2 id="五token-level-的坑那个所有人都漏掉的-part-2">五、Token-level 的坑：那个所有人都漏掉的 Part 2</h2>

<p>到这里为止的推导都是 sequence-level 的。但实现是 token-level 的，而<strong>这中间会掉东西</strong>。</p>

<p>把 k1 的正确梯度展开成 token 形式：</p>

<p>⟦DISPMATH23MATHEND⟧</p>

<p>（中间那步 ⟦INLMATH111MATHEND⟧ 用的是 policy gradient 的 reward-to-go 结论。）</p>

<p><strong>而几乎所有框架的 token-level KL-in-Loss 实现，只算了 Part 1。</strong></p>

<p>Part 2 的含义是：<em>我现在选这个 token，会把后续生成引导到什么样的状态上去，那些状态的 KL 是高是低。</em> 漏掉它，等于在优化时<strong>完全忽略了当前 token 对后续 KL 惩罚的级联影响</strong>——KL 正则从一个序列级的约束退化成了一个<strong>逐 token 的局部近似</strong>。</p>

<p>这一条的杀伤力在于：<strong>它把 4.3 那个漂亮的结论作废了。</strong> k2-in-Loss 在 sequence-level 上是精确无偏的，但一旦按主流方式做 token-level 实现，Part 2 一丢，它<strong>在实现层面重新变回有偏</strong>。</p>

<p>所以六宫格要再补一列：</p>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th> </th>
      <th>IR</th>
      <th>IL (sequence, 理论)</th>
      <th>IL (token, 主流实现)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>k1</td>
      <td>✅ 精确</td>
      <td>❌ 恒为 0</td>
      <td>❌ 恒为 0</td>
    </tr>
    <tr>
      <td>k2</td>
      <td>❌ 有偏</td>
      <td>✅ 精确</td>
      <td>❌ <strong>有偏（漏 Part 2）</strong></td>
    </tr>
    <tr>
      <td>k3</td>
      <td>❌ 有偏</td>
      <td>❌ 有偏</td>
      <td>❌ 有偏</td>
    </tr>
  </tbody>
</table></div>

<p>而 <strong>IR 没有这个问题</strong>——因为 IR 是把 KL 塞进 reward，然后走标准的 policy gradient / advantage 通道，reward-to-go 的累加是 PPO 框架<strong>天然就帮你做了</strong>的。Part 2 在 IR 里是自动包含的。</p>

<p>这是 IR 一个被严重低估的结构性优势：<strong>它不改变优化机制，只改变 reward 数值。</strong> 所有 policy gradient 的正确性结论（reward-to-go、advantage、baseline 降方差）原封不动继承。你想不出错都难。</p>

<hr />

<h2 id="六off-policy-这一层">六、Off-policy 这一层</h2>

<p>前面五节的推导有一个隐藏前提：⟦INLMATH112MATHEND⟧，即数据是当前策略自己采出来的。但 PPO/GRPO 实际是 off-policy 的——数据由 ⟦INLMATH113MATHEND⟧（行为策略，可能是上一轮的 ⟦INLMATH114MATHEND⟧，也可能是切了 mini-batch 之后过时的版本）采出，更新的却是 ⟦INLMATH115MATHEND⟧。标准 PPO 对 reward 项有 importance sampling 比值 ⟦INLMATH116MATHEND⟧，但<strong>KL 项通常没有</strong>——这是一个几乎所有框架都存在的不一致，而且它比”没做 IS”看起来的样子更隐蔽。</p>

<h3 id="61-统一记号把-is-权重也代进梯度里">6.1 统一记号：把 IS 权重也代进梯度里</h3>

<p>为了把 on-policy 和 off-policy 放进同一个框架，定义</p>

<p>⟦DISPMATH24MATHEND⟧</p>

<p>⟦INLMATH117MATHEND⟧ 不含 ⟦INLMATH118MATHEND⟧，天然 stop-grad。关键的地方在于：<strong>on-policy（⟦INLMATH119MATHEND⟧）时 ⟦INLMATH120MATHEND⟧ 这件事只是个数值上的巧合，不是梯度上的。</strong></p>

<p>⟦DISPMATH25MATHEND⟧</p>

<p>代入 on-policy 极限 ⟦INLMATH121MATHEND⟧，得到 ⟦INLMATH122MATHEND⟧。<strong>⟦INLMATH123MATHEND⟧ 的值恒为 1，但它的梯度不是 0。</strong> 这和第二节”KL 梯度必须包含路径导数”是同一个陷阱的另一种形式——只是这次藏在一个看起来像常数的量里。任何直接对 ⟦INLMATH124MATHEND⟧ 反传、却把 ⟦INLMATH125MATHEND⟧ 当常数处理的实现，都会漏掉 ⟦INLMATH126MATHEND⟧ 这一项。</p>

<h3 id="62-这正是-grpo-原始-k3-loss-的-bug-根源">6.2 这正是 GRPO 原始 k3-loss 的 bug 根源</h3>

<p>回头看 4.4：⟦INLMATH127MATHEND⟧（正向 KL 梯度，方向错了）。现在可以把这个结果重新理解一遍：GRPO 原始论文的写法是直接对 ⟦INLMATH128MATHEND⟧ 反传，<strong>完全没有 ⟦INLMATH129MATHEND⟧</strong>——因为大家觉得”反正是 on-policy，⟦INLMATH130MATHEND⟧，乘不乘无所谓”。但根据 6.1，这个想法本身就是错的：即使 on-policy，完整的梯度也应该是 ⟦INLMATH131MATHEND⟧，而不是 ⟦INLMATH132MATHEND⟧。丢掉的正是 ⟦INLMATH133MATHEND⟧ 这一项。</p>

<p>补上这一项会发生什么？用乘积法则展开 ⟦INLMATH134MATHEND⟧（⟦INLMATH135MATHEND⟧，⟦INLMATH136MATHEND⟧，⟦INLMATH137MATHEND⟧）：</p>

<p>⟦DISPMATH26MATHEND⟧</p>

<p>漏掉的那一项和保留的那一项恰好凑成了 ⟦INLMATH138MATHEND⟧。也就是说，<strong>完整的 ⟦INLMATH139MATHEND⟧ 恰好精确等于 ⟦INLMATH140MATHEND⟧</strong>——这正是（把 ⟦INLMATH141MATHEND⟧ 换成 IS 权重之后的）反向 KL 的得分函数梯度。我数值验证过这条恒等式，逐样本残差在浮点精度以内，不是期望意义上的近似。</p>

<p>同样的展开对 ⟦INLMATH142MATHEND⟧（⟦INLMATH143MATHEND⟧ 当常数、只对 ⟦INLMATH144MATHEND⟧ 反传）也成立：⟦INLMATH145MATHEND⟧，所以 ⟦INLMATH146MATHEND⟧——<strong>和完整的 ⟦INLMATH147MATHEND⟧ 逐样本完全相同</strong>，不只是期望相同，是同一个随机变量：</p>

<p>⟦DISPMATH27MATHEND⟧</p>

<p>数值验证（6 类分布，⟦INLMATH148MATHEND⟧ 和 ⟦INLMATH149MATHEND⟧ 显式不同）：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E_old[rho*s*(k1+1)]      与真反向KL梯度误差 = 1.2e-10
E_old[sg(rho)*grad k2]   与真反向KL梯度误差 = 1.2e-10
E_old[rho*grad k3]       与真反向KL梯度误差 = 1.2e-10
sg(rho)*grad_k2 与 rho*grad_k3 的逐样本差   = 0.0  &lt;- 精确恒等，不是期望相等
</code></pre></div></div>

<p>三个东西（⟦INLMATH150MATHEND⟧ 全量反传、⟦INLMATH151MATHEND⟧、⟦INLMATH152MATHEND⟧）都精确收敛到反向 KL 的真梯度，且后两者逐样本恒等。而<strong>去掉 ⟦INLMATH153MATHEND⟧、直接反传裸 ⟦INLMATH154MATHEND⟧</strong>（GRPO 原始写法），on-policy 极限下精确等于正向 KL 梯度（4.4 已证），off-policy 时则既不是正向也不是反向 KL 的梯度，就是一个 generic 的错误量——数值上离两者都更远。</p>

<p>这也解释了 DeepSeek-V3.2 的修复为什么有效：它给 KL 估计器显式乘上 ⟦INLMATH155MATHEND⟧，本质上就是把裸 ⟦INLMATH156MATHEND⟧ 换成 ⟦INLMATH157MATHEND⟧——而这恰好是上面证明的、能精确复原反向 KL 梯度的形式。<strong>这不是”给 off-policy 加了个修正”那么简单，它同时也修好了 on-policy 场景下 GRPO 原始写法里那个被忽略的 ⟦INLMATH158MATHEND⟧ 项。</strong></p>

<p><img src="/assets/img/blog/kl-divergence-code-snippet.webp" alt="KL 估计器 off-policy 修复的代码片段" style="display:block;margin:0 auto;width:95%;max-width:100%;" /></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if args.use_kl_loss:

ref_log_probs = batch["ref_log_probs"]

ref_log_probs = torch.cat(ref_log_probs, dim=0)

importance_ratio = None

if args.use_unbiased_kl:

importance_ratio = torch.exp(log_probs - old_log_probs)

kl = compute_approx_kl(

log_probs,

ref_log_probs,

kl_loss_type=args.kl_loss_type,

importance_ratio=importance_ratio,

)

kl_loss = sum_of_sample_mean(kl)



loss = loss + args.kl_loss_coef * kl_loss
</code></pre></div></div>

<p>当然，第五节的坑还在：只要 token-level 展开仍然只算 Part 1，补了 ⟦INLMATH159MATHEND⟧ 也换不回完整无偏——修了一个洞，另一个洞还在。</p>

<h3 id="63-和-43-的呼应一个更大的恒等式家族">6.3 和 4.3 的呼应：一个更大的恒等式家族</h3>

<p>上面这套 ⟦INLMATH160MATHEND⟧ 记号还揭示了一件事：<strong>4.3 节的 ⟦INLMATH161MATHEND⟧，其实是这个 off-policy 恒等式在 ⟦INLMATH162MATHEND⟧ 时的特例。</strong> 把 ⟦INLMATH163MATHEND⟧（on-policy）代入 ⟦INLMATH164MATHEND⟧，得到 ⟦INLMATH165MATHEND⟧-loss ⟦INLMATH166MATHEND⟧ ⟦INLMATH167MATHEND⟧-with-⟦INLMATH168MATHEND⟧-loss ⟦INLMATH169MATHEND⟧——这正是 4.3 算出的 ⟦INLMATH170MATHEND⟧ 的梯度。也就是说，”⟦INLMATH171MATHEND⟧ 的得分函数梯度 ⟦INLMATH172MATHEND⟧”这个表达式，是三种写法（k1-IR、k2-IL、⟦INLMATH173MATHEND⟧k3-IL/sg(⟦INLMATH174MATHEND⟧)k2-IL）共同的落脚点，区别只是从哪条路径到达它。</p>

<p>反过来，这也告诉我们 <strong>loss 和 reward 两种放法，在样本级别可以给出完全相同的梯度随机变量</strong>，但整体更新语义仍有区别：</p>

<ul>
  <li><strong>是否经过 advantage / baseline</strong>：reward 里的 KL 项会被 GRPO/PPO 的 baseline 处理、可能被部分吸收进 advantage 的中心化里；loss 里的 KL 项是独立加的正则梯度，不受 baseline 影响。</li>
  <li><strong>信用分配路径</strong>：loss 是逐 token 局部生效；reward 会随 return 沿时间线往前传播（这正是第五节 Part 2 的来源——reward-to-go 自动把后续的 KL 惨罚记到前面的 token 头上，loss 写法默认没有）。</li>
</ul>

<p>所以”梯度随机变量相同”不等于”训练效果相同”——这也是为什么本文一直强调要连着”放置位置”和”token 展开”一起看，单独讨论”哪个估计器无偏”永远是半个问题。</p>

<h3 id="64-reward-shaping-下的-off-policy结论不变但要小心批内归一化">6.4 Reward Shaping 下的 Off-policy：结论不变，但要小心批内归一化</h3>

<p>第四节证明的”只有 k1-IR 精确无偏”这个结论，off-policy 下要不要重新证一遍？其实不用。标准 PPO 的做法是把 IS 比值 ⟦INLMATH175MATHEND⟧ 乘在<strong>整条</strong>得分函数项上（reward 和折进 reward 里的 KL 一起乘），这个比值和”KL 估计器本身选哪个”“路径导数期望是否为零”是两件独立的事——k1 的路径导数恒为 0 这个论证全程没用到 on-policy 假设，off-policy 下继续成立。所以结论照搬：<strong>reward shaping 下 off-policy 也只有 k1 精确无偏，k2/k3 折进 reward 一样会带进那个符号相反的正向 KL 污染项。</strong></p>

<p>唯一需要额外小心的是一个和 KL 估计器本身无关、但经常和它绑在一起出现的坑：<strong>GRPO 式的组内（group-relative）reward 归一化</strong>。如果对 shaped reward ⟦INLMATH176MATHEND⟧ 做”减去批内均值再除以批内标准差”，且均值/方差的计算<strong>包含了当前这条样本自己</strong>，会引入 ⟦INLMATH177MATHEND⟧ 的系统性偏差（自己被自己的统计量除了一次）。这个偏差和 KL 估计器无偏与否叠加在一起，会让”我明明用了 k1-IR”却依然对不上手推结论。修法很直接：<strong>用 leave-one-out 的均值/方差</strong>（算统计量时去掉当前样本），既能保持严格无偏，又不损失方差缩减的效果。这条和 KL 本身的偏差无关，但因为归一化和 KL 项通常写在同一段代码里，经常被一起冤枉。</p>

<hr />

<h2 id="七真正能落地的-takeaway">七、真正能落地的 Takeaway</h2>

<p>前面全是推导，这一节讲怎么用。先给一张速查表，后面再展开讲每一行的理由。</p>

<h3 id="71-速查表">7.1 速查表</h3>

<div class="table-responsive"><table class="table table-sm">
  <thead>
    <tr>
      <th>场景</th>
      <th>推荐写法</th>
      <th>一句话理由</th>
      <th>出处</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>On-policy，KL in Reward</td>
      <td><strong>k1</strong></td>
      <td>路径导数期望恒为 0，精确无偏，且对 stop-grad 免疫</td>
      <td>§4.1</td>
    </tr>
    <tr>
      <td>On-policy，KL in Loss</td>
      <td><strong>k2</strong>；或显式写出 ⟦INLMATH178MATHEND⟧ / ⟦INLMATH179MATHEND⟧</td>
      <td>k2-loss 精确等价于 k1-in-Reward；裸 k3-loss（不带 ⟦INLMATH180MATHEND⟧）会退化成<strong>正向 KL</strong> 梯度，方向错了，不是大小错了</td>
      <td>§4.3、§4.4、§6.2</td>
    </tr>
    <tr>
      <td>Off-policy，KL in Reward</td>
      <td><strong>k1</strong>，套标准 PPO 整条 IS 比值即可，KL 项不用再单独加权</td>
      <td>k1 路径导数为零这个论证不依赖 on-policy 假设</td>
      <td>§6.4</td>
    </tr>
    <tr>
      <td>Off-policy，KL in Loss</td>
      <td><strong>⟦INLMATH181MATHEND⟧ 或 ⟦INLMATH182MATHEND⟧</strong>（逐样本恒等，选哪个纯看实现方便）</td>
      <td>唯二能在 off-policy 下精确复原反向 KL 真梯度的写法；裸 ⟦INLMATH183MATHEND⟧ 虽也无偏但方差大得多</td>
      <td>§6.1、§6.2</td>
    </tr>
    <tr>
      <td>监控 / 画曲线 / early stop 判据</td>
      <td><strong>k3</strong>（或算力够就上 full-vocab 精确 KL）</td>
      <td>无偏 + 非负 + 低方差三者都要；和梯度走哪条路径完全独立</td>
      <td>§7.4</td>
    </tr>
  </tbody>
</table></div>

<p>表里没写但必须记住的两条横切规则：<strong>token-level 实现要有 Part 2</strong>（§5，否则 loss 一列全部退化）；<strong>离 ⟦INLMATH184MATHEND⟧ 越远，表里所有”近似成立”的结论越不成立</strong>（§7.5）。</p>

<h3 id="72-默认选择">7.2 默认选择</h3>

<blockquote>
  <p><strong>默认用 k1-in-Reward。</strong></p>
</blockquote>

<p>理由不是它数学上最优雅，而是它<strong>在工程上最难写错</strong>：</p>

<ol>
  <li>梯度精确无偏，不是近似；</li>
  <li>对 stop-grad 免疫（4.1 的推论）——log 项包不包 <code class="language-plaintext highlighter-rouge">no_grad</code> 都一样，少一个出错点；</li>
  <li>token-level 的 Part 2 由 PPO 的 reward-to-go 自动包含，不需要你手写；</li>
  <li>不改变优化机制，advantage / baseline / value function 全部原样复用。</li>
</ol>

<p>k1 唯一的缺点是<strong>值的方差大</strong>，但注意——<strong>这个缺点在 IR 里被大幅稀释了</strong>：KL 是加进 reward 再过 advantage 和 baseline 的，PPO 的方差缩减机制本来就在处理这个。你不是拿单样本 k1 直接当梯度用。</p>

<h3 id="73-如果你的框架只能-kl-in-loss">7.3 如果你的框架只能 KL-in-Loss</h3>

<p>很多框架（GRPO 系）就是 in-loss 的，改不动。这里要分 on-policy 和 off-policy 两种情况——它们的正确答案不一样。</p>

<p><strong>On-policy（⟦INLMATH185MATHEND⟧，或者干脆没有 ⟦INLMATH186MATHEND⟧ 这个概念）：</strong></p>

<ul>
  <li><strong>绝对不要用 k1</strong>。它梯度期望为 0，等于没加正则还平白增加方差。这是最容易犯且最隐蔽的错。</li>
  <li><strong>理论最优是 k2</strong>，它 sequence-level 精确等价于 k1-IR。</li>
  <li><strong>如果直接用裸 k3 反传，要知道你在正则正向 KL，不是反向 KL</strong>（§4.4、§6.2）——除非你显式把 ⟦INLMATH187MATHEND⟧（哪怕 on-policy 下 ⟦INLMATH188MATHEND⟧，见 §6.1）乘进去再反传，那样才会精确落回反向 KL 的梯度。</li>
  <li><strong>如果能改实现，把 Part 2 补上</strong>——具体做法是让 token ⟦INLMATH189MATHEND⟧ 的 KL 系数变成从 ⟦INLMATH190MATHEND⟧ 到 ⟦INLMATH191MATHEND⟧ 的 KL 累加（reward-to-go 形式），而不是只用 token ⟦INLMATH192MATHEND⟧ 自己的 KL。这是把 IL 修对的唯一办法。</li>
</ul>

<p><strong>Off-policy（mini-batch 复用、⟦INLMATH193MATHEND⟧）：</strong></p>

<ul>
  <li><strong>用 ⟦INLMATH194MATHEND⟧ 或 ⟦INLMATH195MATHEND⟧</strong>（§6.2 已证逐样本恒等），两者都能精确复原反向 KL 的真梯度。裸 k3（不带 ⟦INLMATH196MATHEND⟧）此时既不是反向也不是正向 KL 的梯度，纯粹是个 generic 的错误量，比 on-policy 下的”至少还是个正确的正向 KL 梯度”更糟。</li>
  <li>DeepSeek-V3.2 的修复本质上就是把 GRPO 的裸 ⟦INLMATH197MATHEND⟧-loss 换成 ⟦INLMATH198MATHEND⟧——这一步同时修好了”off-policy 没做 IS”和”on-policy 也漏掉 ⟦INLMATH199MATHEND⟧”两个问题，虽然论文的表述强调的是前者。</li>
</ul>

<p>顺带回答一个自然的疑问：<strong>既然 k2 理论上最优，为什么框架默认给的都是 k3？</strong> 除了路径依赖（DeepSeekMath 的 GRPO 公式里写的就是 k3，后面全抄）之外，有两个不那么显然的实质理由：</p>

<ul>
  <li><strong>非负性在 loss 里是硬需求。</strong> ⟦INLMATH200MATHEND⟧ 意味着优化器不可能靠”把 KL 项推成负数”来刷低 loss。k1 在 loss 里可以无限往负漂（尽管期望为 0），这在有限 batch 上是真实的失效模式。k2 虽然也非负，但它是平方项，对 outlier token（⟦INLMATH201MATHEND⟧ 很大的那些）的梯度响应比 k3 的线性响应更剧烈。</li>
  <li><strong>无偏从来不是 RL 里的最高优先级。</strong> 无偏只是对无穷样本的承诺，有限 batch 下决定优化质量的是 MSE = 偏差² + 方差。k1 的方差是信号的几十上百倍，k3 只有 1.6 倍。<strong>用一点偏差换两个数量级的方差，几乎总是划算的。</strong>（<code class="language-plaintext highlighter-rouge">verl</code> 的 k3+ 实现——前向算 k3 的非负值当监控，反向换成 k2 的梯度——就是把”数值要 k3、梯度要 k2”这个诉求直接写进了框架里，参见 §4 六宫格下面的说明。）</li>
</ul>

<h3 id="74-值和梯度要分开选">7.4 值和梯度要分开选</h3>

<p>这是我认为最有实操价值的一条，也是很多人没意识到的：</p>

<blockquote>
  <p><strong>监控用的 KL 和参与梯度的 KL，不必是同一个估计器。</strong></p>
</blockquote>

<ul>
  <li><strong>打日志、画曲线、做 early stop 判据</strong> → 用 <strong>k3</strong>（无偏 + 非负 + 低方差，三个都要）。别用 k1，它单样本方差是信号的几十上百倍，你画出来的是噪声。</li>
  <li><strong>参与反向传播</strong> → 按 7.1 速查表 / 7.2 / 7.3 选。</li>
</ul>

<p>这两件事在代码里就应该是两个独立的量。<strong>把它们混为一谈，是 k3 被误用到 reward 里的根本原因</strong>——大家看 k3 值算得准，就以为放哪儿梯度都准。</p>

<p>另外，如果显存和算力允许，<strong>监控可以直接算 full-vocab 的逐 token 精确 KL</strong>（对词表求和而不是单样本），它对序列 KL 无偏且方差极低。但注意：<strong>这只解决值的方差问题，不解决梯度缺项问题</strong>——它进 loss 时同样缺得分函数项 / Part 2。</p>

<h3 id="75-什么时候偏差可以忽略什么时候必须小心">7.5 什么时候偏差可以忽略，什么时候必须小心</h3>

<p><strong>可以忽略的场景：</strong></p>
<ul>
  <li>⟦INLMATH202MATHEND⟧ 很小，KL 只是个弱兜底（现在很多 reasoning RL 干脆 ⟦INLMATH203MATHEND⟧）。这时候你争论哪个估计器无偏没有意义。</li>
  <li>训练全程 KL 稳定在很小的量级（比如 &lt;0.01），且<strong>没有出现漂移趋势</strong>。此时 k2/k3 之间、IR/IL 之间的差别都是高阶小量。</li>
  <li>短序列。Part 2 的缺失量随序列长度增长，短序列上影响有限。</li>
</ul>

<p><strong>必须小心的场景：</strong></p>
<ul>
  <li><strong>长 CoT / 长序列生成。</strong> Part 2 漏掉的是 ⟦INLMATH204MATHEND⟧ 项的累加，⟦INLMATH205MATHEND⟧ 越大漏得越多。这是当前 reasoning RL 最相关的场景。</li>
  <li><strong>⟦INLMATH206MATHEND⟧ 较大、真的指望 KL 拴住模型。</strong> 这时 k3-IR 那个”一阶抵消”会直接坑你：你以为拴住了，其实绳子是软的。</li>
  <li><strong>KL 已经涨起来了（&gt;0.1 量级）。</strong> 所有”⟦INLMATH207MATHEND⟧ 的近似”全部失效：k2 值偏 17%，而 k3-IL 那个”正反向 KL 等价”的理由也不再成立——此时你是在实打实地正则一个和你以为的不同的散度（相对误差可达 30%）。<strong>注意这是个恶性循环</strong>：KL 一涨，估计器就变差，正则就更弱，KL 涨得更快。</li>
  <li><strong>off-policy 程度高</strong>（一批数据复用多个 epoch、mini-batch 切得细、⟦INLMATH208MATHEND⟧ 和 ⟦INLMATH209MATHEND⟧ 拉开）。裸估计器（不带 ⟦INLMATH210MATHEND⟧）此时既不是反向也不是正向 KL 的梯度，比 on-policy 下的”至少还是个合法散度”更糟（§6.2）。</li>
  <li><strong>换框架 / 换 KL 实现之后调不出原来的效果。</strong> 大概率不是超参问题，是你从六宫格的一格换到了另一格，有效正则强度整个变了。<strong>⟦INLMATH211MATHEND⟧ 在不同格子之间不可迁移。</strong></li>
</ul>

<h3 id="76-一条自检清单">7.6 一条自检清单</h3>

<p>改动任何 KL 相关代码后，问自己五个问题：</p>

<ol>
  <li>我用的是哪个估计器？</li>
  <li>它是进 reward 还是进 loss？（等价于问：stop-grad 打在采样分布上还是 log 项上？）</li>
  <li>如果进 loss，token-level 展开有没有 Part 2？</li>
  <li>数据是不是 off-policy？如果是，重要性权重 ⟦INLMATH212MATHEND⟧ 有没有乘进 KL 项——而且是<strong>不 stop-grad</strong> 地乘进去（§6.1，⟦INLMATH213MATHEND⟧ 时它的梯度也不是 0）？</li>
  <li>如果这条数据是当作 reward 用的，批内归一化（如果有）算没算进当前样本自己（§6.4，留一法 vs 直接算）？</li>
</ol>

<p>五个答案凑齐了，才能说清楚自己的 KL 正则到底是什么。<strong>只回答第 1 个问题——”我用的是 k3，无偏的”——是没有意义的。</strong></p>

<hr />

<h2 id="八一句话总结">八、一句话总结</h2>

<p>如果只能记一句：</p>

<blockquote>
  <p><strong>“这个估计器是不是无偏的”是个病态问题。正确的问法是”这个估计器 × 这种放法 × 这种 token 展开，梯度是不是无偏的”。而在这三个维度上都最稳的答案，是 k1-in-Reward。</strong></p>
</blockquote>

<p>有点反讽的是：<strong>最朴素、方差最大、看起来最不该用的 k1，配上最老的 InstructGPT 式放法，反而是唯一一个从头到尾都严格正确的组合。</strong> k2/k3 那些精巧的方差缩减设计，优化的是”值”这个我们其实不太在乎的量——而它们对”梯度”这个我们真正在乎的量的影响，完全取决于被放在哪一格。</p>

<p>不过要给 k3 说句公道话，这也是我写完这篇才想清楚的：<strong>k3 的流行不是因为大家没注意到它不是真梯度，而是因为它被用在了 IL 这个”错得最无害”的位置上。</strong> 在那里它等价于精确的正向 KL 正则——一个合法的、只是方向和设计初心（反向 KL）不同的散度，配上非负性和低方差，在 ⟦INLMATH214MATHEND⟧ 的工作区间内是个相当合理的工程选择；而 off-policy 时（第六节）它连”精确的另一个散度”都不是，必须补上重要性权重 ⟦INLMATH215MATHEND⟧ 才能救回来。</p>

<p>真正该警惕的是另外两格：<strong>k3/k2-in-Reward</strong>（正反向 KL 一阶抵消，正则形同虚设）和 <strong>k1-in-Loss</strong>（梯度恒为 0）。这两格才是会让你”以为加了 KL 其实没加”的陷阱——而且它们不报错、曲线看着还挺正常。</p>

<p>繁华落尽见真淳。</p>

<hr />

<h2 id="附录-a对数技巧">附录 A：对数技巧</h2>

<p>全文反复用到：</p>

<p>⟦DISPMATH28MATHEND⟧</p>

<p>由 ⟦INLMATH216MATHEND⟧ 两边乘 ⟦INLMATH217MATHEND⟧ 移项即得。</p>

<p>它的两个直接推论撑起了整篇文章：</p>
<ul>
  <li>⟦INLMATH218MATHEND⟧ → k1 路径导数消失</li>
  <li>⟦INLMATH219MATHEND⟧ → k3 的控制变量项无偏</li>
</ul>

<h2 id="附录-b数值验证">附录 B：数值验证</h2>

<p>用一个 ⟦INLMATH220MATHEND⟧ 类离散分布（softmax 参数化）暴力验证六宫格的每一格，以及 k3-IR 的正向 KL 恒等式：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="n">K</span> <span class="o">=</span> <span class="mi">6</span>
<span class="n">softmax</span> <span class="o">=</span> <span class="k">lambda</span> <span class="n">z</span><span class="p">:</span> <span class="p">(</span><span class="k">lambda</span> <span class="n">e</span><span class="p">:</span> <span class="n">e</span><span class="o">/</span><span class="n">e</span><span class="p">.</span><span class="nf">sum</span><span class="p">())(</span><span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">z</span> <span class="o">-</span> <span class="n">z</span><span class="p">.</span><span class="nf">max</span><span class="p">()))</span>

<span class="n">zr</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">K</span><span class="p">);</span> <span class="n">zt</span> <span class="o">=</span> <span class="n">zr</span> <span class="o">+</span> <span class="mf">0.3</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">K</span><span class="p">)</span>
<span class="n">p</span><span class="p">,</span> <span class="n">q</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">zt</span><span class="p">),</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">zr</span><span class="p">)</span>
<span class="n">G</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">K</span><span class="p">)</span> <span class="o">-</span> <span class="n">p</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>            <span class="c1"># G[y] = grad_z log p(y)
</span><span class="n">s</span> <span class="o">=</span> <span class="n">q</span><span class="o">/</span><span class="n">p</span>
<span class="n">k1</span><span class="p">,</span> <span class="n">k2</span><span class="p">,</span> <span class="n">k3</span> <span class="o">=</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">s</span><span class="p">),</span> <span class="mf">0.5</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">s</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">+</span> <span class="p">(</span><span class="n">s</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

<span class="n">score</span> <span class="o">=</span> <span class="k">lambda</span> <span class="n">c</span><span class="p">:</span> <span class="p">(</span><span class="n">p</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">c</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">G</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>   <span class="c1"># IR: E[c * grad log p]
</span><span class="n">IR</span> <span class="o">=</span> <span class="p">{</span><span class="sh">'</span><span class="s">k1</span><span class="sh">'</span><span class="p">:</span> <span class="nf">score</span><span class="p">(</span><span class="n">k1</span><span class="p">),</span> <span class="sh">'</span><span class="s">k2</span><span class="sh">'</span><span class="p">:</span> <span class="nf">score</span><span class="p">(</span><span class="n">k2</span><span class="p">),</span> <span class="sh">'</span><span class="s">k3</span><span class="sh">'</span><span class="p">:</span> <span class="nf">score</span><span class="p">(</span><span class="n">k3</span><span class="p">)}</span>
<span class="n">IL</span> <span class="o">=</span> <span class="p">{</span><span class="sh">'</span><span class="s">k1</span><span class="sh">'</span><span class="p">:</span> <span class="p">(</span><span class="n">p</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">G</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>                      <span class="c1"># grad k1 = grad log p
</span>      <span class="sh">'</span><span class="s">k2</span><span class="sh">'</span><span class="p">:</span> <span class="p">(</span><span class="n">p</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">k1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">G</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span>        <span class="c1"># grad k2 = k1 * grad log p
</span>      <span class="sh">'</span><span class="s">k3</span><span class="sh">'</span><span class="p">:</span> <span class="p">(</span><span class="n">p</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">s</span><span class="p">)[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">G</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">)}</span>     <span class="c1"># grad k3 = (1-s) * grad log p
</span></code></pre></div></div>

<p>把上面的结果和有限差分算出的真梯度 ⟦INLMATH221MATHEND⟧ 对比，得到：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>--- 距离大    KL=0.07915  |真梯度|=1.763e-01
   k1_IR   |g|=1.763e-01   误差=8.8e-11   &lt;- 精确
   k2_IR   |g|=3.461e-02   误差=1.6e-01
   k3_IR   |g|=3.042e-02   误差=1.6e-01
   k1_IL   |g|=3.4e-17     误差=1.8e-01   &lt;- 恒为 0
   k2_IL   |g|=1.763e-01   误差=8.8e-11   &lt;- 精确
   k3_IL   |g|=1.646e-01   误差=3.0e-02
   校验 k3_IR == 真梯度 - 正向KL梯度 ? 残差 2.0e-10   &lt;- 恒等式成立

--- 距离很小  KL=0.00122  |真梯度|=3.025e-02
   k1_IR   |g|=3.025e-02   误差=1.2e-10
   k3_IR   |g|=2.251e-04   &lt;- 只有真梯度的 0.74%，正则几乎消失
</code></pre></div></div>

<p>再单独验证 k3-IL 的身份（4.4 的第一个 boxed 式）：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>=== k3_IL vs 正向 KL 梯度 ===
eps=1.00 : |k3_IL - grad KL(ref||th)| = 1.8e-10    k3_IL 相对真梯度误差 = 31.21%
eps=0.30 : |k3_IL - grad KL(ref||th)| = 1.5e-10    k3_IL 相对真梯度误差 = 16.20%
eps=0.10 : |k3_IL - grad KL(ref||th)| = 8.2e-11    k3_IL 相对真梯度误差 =  5.36%
eps=0.03 : |k3_IL - grad KL(ref||th)| = 1.2e-10    k3_IL 相对真梯度误差 =  0.63%
</code></pre></div></div>

<p>左列的残差<strong>在任何距离下都是浮点噪声</strong>——k3-IL 恒等于正向 KL 梯度，不是近似。右列则显示它和反向 KL 真梯度的差距随距离缩小而消失。</p>

<p>三个结论被精确验证：</p>

<ol>
  <li><strong>k1_IR 和 k2_IL 的误差在 ⟦INLMATH222MATHEND⟧ 量级</strong>（浮点噪声），是恒等而非近似；</li>
  <li><strong>k3_IL ⟦INLMATH223MATHEND⟧（正向 KL 梯度）</strong>，残差 ⟦INLMATH224MATHEND⟧，而它相对反向 KL 真梯度的误差 → 0.63%；</li>
  <li><strong>k3_IR 的梯度模长随 ⟦INLMATH225MATHEND⟧ 迅速塌陷到真梯度的 1% 以下</strong>——第 4.4 节”一阶抵消”的直接证据。</li>
</ol>

<p>第 2 条和第 3 条并列看，就是 4.4 末尾那张表想说的事：同一个 k3，IL 位置上”只差 0.6%”，IR 位置上”只剩 0.7%”。</p>

<h2 id="参考">参考</h2>

<ol>
  <li><strong>On a few pitfalls in KL divergence gradient estimation for RL.</strong> <a href="https://arxiv.org/abs/2506.09477">arxiv 2506.09477</a> —— 最早给出正确梯度形式并系统分析业界做法的工作，主要面向 in-loss。</li>
  <li><strong>Rethinking KL Regularization in RLHF: From Value Estimation to Gradient Optimization.</strong> <a href="https://arxiv.org/abs/2510.01555">arxiv 2510.01555</a> —— 细节充分，但缺 token-level 分析。本文”标红 ⟦INLMATH226MATHEND⟧”的记法来自这里。</li>
  <li><strong>A Comedy of Estimators: On KL Regularization in RL Training of LLMs.</strong> <a href="https://arxiv.org/abs/2512.21852">arxiv 2512.21852</a> —— Bengio 组，ICLR’26 被拒。</li>
  <li>John Schulman, <em>Approximating KL Divergence</em> —— k3 的出处。</li>
  <li><a href="https://zhuanlan.zhihu.com/p/2019518994252607760">繁华落尽见真淳：LLM 强化学习中 KL 散度的正确形式是 k1 in Reward</a> —— 六宫格、Part 2、”值 vs 梯度”这条主线的分析来自这篇。</li>
  <li><a href="https://xihuai18.github.io/reinforcement-learning/2025/12/01/kl-estimators-zh.html">RL 中的 KL 估计器选型：从数值无偏到梯度正确</a> —— 第六节的 ⟦INLMATH227MATHEND⟧ 记号、on/off-policy 统一梯度框架、⟦INLMATH228MATHEND⟧ 恒等式、reward shaping 下的批内归一化陷阱，都来自这篇。本文”反向/正向 KL”的命名约定也是照这篇（以及标准变分推断惯例）统一过来的。</li>
</ol>]]></content><author><name></name></author><category term="reinforcement-learning" /><summary type="html"><![CDATA[从"无偏"和"有偏"的判断标准出发，拆解 k1/k2/k3 三种 KL 估计器在 KL-in-Reward 和 KL-in-Loss 两种实现下的梯度行为，说明"有偏性是估计器和放置位置组合的属性，不是估计器本身的属性"。]]></summary></entry></feed>