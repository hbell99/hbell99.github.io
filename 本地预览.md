# 本地预览网站

每次开新终端窗口，先设置 Ruby 环境变量（本机系统自带的 Ruby 太老，要用 Homebrew 装的新版）：

```bash
export PATH="/opt/homebrew/opt/ruby/bin:/opt/homebrew/lib/ruby/gems/4.0.0/bin:$PATH"
```

然后进入项目目录，启动本地服务器：

```bash
cd ~/Documents/个人主页
bundle exec jekyll serve
```

浏览器打开 <http://localhost:4000> 就能看到效果。改动文件保存后会自动重新构建，刷新页面即可看到最新效果。

按 `Ctrl+C` 停止服务器。
